# HANDOFF — M24 deferred slate (round 49): helper-level direction assertions on the round-47/48 stalled fixtures — three new `@Test` methods drive each stalled fixture (50%-alternating baseline, 0%-all-off boundary, 100%-all-on boundary) through `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)` and assert the helper output directly (`.progress?.direction == .stalled`, exact `earlierRate` / `recentRate` boundary values, `windowSize == 2`, `.resolved == nil`, and the round-43 scenario+tone anchoring titles). Round 49 closes future move #30 from round 48's HANDOFF ("Helper-level test that `toneDrillProgress` returns `direction == .stalled` for the round-47 + round-48 fixtures") and adds the defensive belt the round-47/48 cross-surface tests don't carry: a future regression that drifts `IMHistorySummary.toneDrillProgressThreshold` (currently `0.15`) silently — say to `0.05`, which would let a fixture with `delta == 0.1` read as recovering/slipping — would still trip the round-47/48 surface tests on the existing three fixtures (delta is exactly 0 on all three, well within any plausible threshold), but would NOT catch the helper-level regression that broke ONLY direction classification (without breaking the surface templates). Round 49 pins the helper-level boundary at the helper output for each fixture shape, so a direction-classification regression at any rate point is caught at the helper boundary before either downstream surface gets a chance to silently swallow the change. Pure test addition; no production code change, no engine state change, no schema bump, no migration, no new view inputs.

## Scope

Round 48 closed the boundary completion on the stalled corner of the 2×2 direction × locus matrix (50%-alternating baseline + 0%-all-off boundary + 100%-all-on boundary + 50%-alternating + parallel-prior-crossing). The four gaps it explicitly carried forward as future moves:

- **Future move #29** (round 48 HANDOFF): "Helper-output assertion layered into the cross-surface tests. The round-46 + round-47 + round-48 baseline tests assert the *prompt + note text* don't name the parallel scenario, which is the user-facing contract. A defensive belt would also assert at the helper output: `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:).progress == nil && .resolved == nil` for the thin-A justFinished (round 46), `.progress?.direction == .stalled && .resolved == nil` for the stalled-A justFinished (rounds 47 + 48), AND `.progress?.earlierRate == 0.0` / `.progress?.earlierRate == 1.0` for the round-48 boundary fixtures."
- **Future move #30** (round 48 HANDOFF): "Helper-level test that `toneDrillProgress` returns `direction == .stalled` for the round-47 + round-48 fixtures. The round-47/48 cross-surface tests assert the surface outputs don't contain 'tone-match'; a helper-level test asserting `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:).progress?.direction == .stalled` for the same fixtures (50%-alternating, 0%-all-off, 100%-all-on) would catch a helper regression that shifted the stalled boundary silently (e.g., the threshold drifts to 0.05 and a fixture starts reading as recovering or slipping). Defensive belt; hold for the right round."
- **Future move #31** (round 48 HANDOFF): "Stalled-trajectory window-size = 2 vs window-size = 3 cross-surface contract. The round-47/48 stalled fixtures all use 4 evaluated reps (`window = min(3, 4/2) = 2`). The next granularity is 6+ reps (`window = min(3, 6/2) = 3`), which exercises the wider-window stalled gate. Defensive companion; hold for the right round."
- **Future move #32** (round 48 HANDOFF): "Stalled-trajectory at the boundary of `toneDrillProgressThreshold`. The round-47/48 stalled fixtures have `delta == 0` (the cleanest stalled signal). A defensive cross-surface test at the exact boundary (`delta == ±toneDrillProgressThreshold - ε`) would lock the gate on its tightest discriminator. Hold for the right round."

Round 49 closes #30 head-on, AND partially overlaps with #29 (the helper-output assertion on the round-48 boundary fixtures) — the new three tests carry the exact `earlierRate` (0.0 / 0.5 / 1.0) and `recentRate` (0.0 / 0.5 / 1.0) boundary assertions that future move #29 calls out for the round-48 boundary fixtures, plus the `windowSize == 2` and the SOLVED `.resolved == nil` companions, plus the round-43 scenario+tone anchoring titles. So round 49 closes #30 entirely and ~60% of #29 (the round-48 boundary fixture leg). The thin-A round-46 leg of #29 (`.progress == nil && .resolved == nil`) stays deferred for a future round; the round-47 50%-alternating leg of #29 is covered by round 49 test 1. Three new `@Test` methods on `PostRepCoachNoteUserPromptAnchoringTests`, slotted at the end of the suite immediately after the round-48 `userPromptAndDeterministicNoteFullyOnBrandStalledJustFinishedNeverProducesOwnTrajectoryClause` test and immediately before the closing brace, under a new `// MARK: - Helper-level direction assertions on round-47/48 stalled fixtures (round 49)` section:

1. **`helperReturnsStalledDirectionForAlternating50PercentStalledFixture`** — 50%-stalled own-scenario. Same fixture as round 47's baseline (`["shaky", "confident", "shaky", "confident"]` in scenario A). Helper drives `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)` and asserts: `progress?.direction == .stalled`, `progress?.earlierRate == 0.5`, `progress?.recentRate == 0.5`, `progress?.windowSize == 2`, `resolved == nil`, `resolvedScenarioTitle == nil`, `resolvedToneTitle == nil`, `scenarioTitle == "Networking"`, `toneTitle == "Confident"`.
2. **`helperReturnsStalledDirectionForFullyOffBrand0PercentStalledFixture`** — 0%-stalled own-scenario. Same fixture as round 48's test 2 (`["shaky", "shaky", "shaky", "shaky"]` in scenario A). Helper asserts: `progress?.direction == .stalled`, `progress?.earlierRate == 0.0`, `progress?.recentRate == 0.0`, `progress?.windowSize == 2`, `resolved == nil`, `resolvedScenarioTitle == nil`, `resolvedToneTitle == nil`, `scenarioTitle == "Networking"`, `toneTitle == "Confident"`.
3. **`helperReturnsStalledDirectionForFullyOnBrand100PercentStalledFixture`** — 100%-stalled own-scenario. Same fixture as round 48's test 3 (`["confident", "confident", "confident", "confident"]` in scenario A). Helper asserts: `progress?.direction == .stalled`, `progress?.earlierRate == 1.0`, `progress?.recentRate == 1.0`, `progress?.windowSize == 2`, `resolved == nil`, `resolvedScenarioTitle == nil`, `resolvedToneTitle == nil`, `scenarioTitle == "Networking"`, `toneTitle == "Confident"`.

The three tests together pin the helper-level stalled-boundary contract directly at the helper output for each fixture shape the round-47/48 surface tests drive end-to-end through the prompt + deterministic note surfaces: a future regression that broke ONLY direction classification (e.g., a threshold drift), without breaking the surface templates, would still be caught at the helper boundary before either surface silently swallowed the change. The SOLVED-gate `resolved == nil` companions assert the two-arm gate symmetrically: 50%-stalled fails the earlier-below-bar arm (`earlierRate (0.5) NOT < matchRateThreshold (0.4)`); 0%-stalled fails the recent-hold arm (`recentRate (0.0) NOT >= toneDrillResolvedHoldRate (0.6)`); 100%-stalled fails the earlier-below-bar arm (`earlierRate (1.0) NOT < matchRateThreshold (0.4)`). The round-43 scenario+tone anchoring titles assert the helper threads ONLY the just-finished rep's scenario through, never a global "loudest scenario" read.

Defensive scoping (intentional restraint, same shape as rounds 41/42/43/44/45/46/47/48):

- **Pure test addition, no production code change.** Round 49 modifies only `NoumTests/NoumTests.swift` and `HANDOFF.md`. Every production surface (`MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)`, `PostRepCoachNoteService.userPrompt(from:)`, `PostRepCoachNoteService.deterministicNote(input:)`, `IMHistorySummary.toneDrillProgress(from:scenario:)`, `IMHistorySummary.toneDrillCrossing(...)`, `IMHistorySummary.toneDrillResolved(...)`) is preserved verbatim. The helper-level contract round 49 pins was already true at rounds 12/14 (the `IMHistorySummary.toneDrillProgress` primitive's stalled gate via `delta > -toneDrillProgressThreshold && delta < toneDrillProgressThreshold`); round 49 locks it at the helper output (`MomentumComputer.imToneDrillNoteFields(...)`) for each round-47/48 fixture shape.
- **No new freshness gate.** Same rationale as rounds 43/44/45/46/47/48: the post-rep note is written EXACTLY ONCE, immediately after the rep finalizes, against signals computed strictly for the just-finished rep's scenario. The per-rep anchoring IS the freshness contract on both the deterministic and AI paths. Round 49 does NOT introduce a recency window.
- **No engine state change.** All `IMHistorySummary` helpers, `MomentumComputer.imToneDrillNoteFields`, both surface bodies — unchanged. No new persisted state, no schema bump, no migration, no new view inputs.
- **`MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)` is `static`.** Round 49 drives the helper directly without finalizer plumbing — the same testability lever rounds 43–48 used. No mock provider, no finalizer wire, no rate-limit gymnastics.
- **Test fixtures mirror rounds 47/48 verbatim.** `imSession(id:scenario:targetTone:actualTone:daysOffset:)` is reused unchanged; the three stalled-A fixtures (50%-alternating, 0%-all-off, 100%-all-on) are built inline using the same shape the round-47/48 surface tests use. No new helper.
- **Tests pin BOTH the direction classification AND the boundary rate values.** A regression that broke ONLY the direction classification (e.g., threshold drift) trips the direction assertion; a regression that broke ONLY the window-split arithmetic (e.g., earlier prefix extended to 3 reps) trips the rate-value assertions even if the direction still read as stalled. Two independent failure modes pinned in one assertion block per fixture.
- **Each test drives the helper ONLY.** No prompt assertion, no deterministic note assertion — the round-47/48 cross-surface tests already pin those. Round 49 is purely the helper-boundary defensive belt; the surface tests + the helper-boundary tests together form a two-layer regression net.

User brief, unchanged round to round: "continue from the existing TO-DO, ensure working towards getting the app towards the vision plan, and all round A+, make my dream I had come true too, ensure working on the redesign branch too (very important)."

Translation, this round:

- Three new `@Test` methods (`helperReturnsStalledDirectionForAlternating50PercentStalledFixture`, `helperReturnsStalledDirectionForFullyOffBrand0PercentStalledFixture`, `helperReturnsStalledDirectionForFullyOnBrand100PercentStalledFixture`) appended to the existing `PostRepCoachNoteUserPromptAnchoringTests` suite, slotted immediately after round 48's `userPromptAndDeterministicNoteFullyOnBrandStalledJustFinishedNeverProducesOwnTrajectoryClause` test under a new `// MARK: - Helper-level direction assertions on round-47/48 stalled fixtures (round 49)` section so the recovering / crossing / thin / stalled cross-surface contracts AND the helper-boundary defensive belt read as one contiguous block. No new fixtures.
- The redesign-branch invariant: round 49 is a `Redesign`-branch push per the user brief. It preserves the round-by-round loop on the redesign lineage that has been the home of rounds 11–48.

## What shipped

### Track 1 — Three helper-level direction assertion tests on round-47/48 stalled fixtures (`NoumTests/NoumTests.swift`)

Three new `@Test` methods on the existing `struct PostRepCoachNoteUserPromptAnchoringTests` suite (the round-44/45/46/47/48 suite, now 27 tests), slotted under a new `// MARK: - Helper-level direction assertions on round-47/48 stalled fixtures (round 49)` section immediately after the existing round-48 `userPromptAndDeterministicNoteFullyOnBrandStalledJustFinishedNeverProducesOwnTrajectoryClause` test and immediately before the closing brace of the suite. The stalled-A fixtures (50% alternating in test 1, 100% all-off in test 2, 100% all-on in test 3) are built inline and reuse the round-47/48 shape verbatim.

- **`helperReturnsStalledDirectionForAlternating50PercentStalledFixture`** — 50%-stalled own-scenario. Just-finished rep is the 4th rep in scenario A (Networking, confident) with alternating `["shaky", "confident", "shaky", "confident"]`. Drives the helper directly and asserts: `fields.progress?.direction == .stalled`, `fields.progress?.earlierRate == 0.5`, `fields.progress?.recentRate == 0.5`, `fields.progress?.windowSize == 2`, `fields.resolved == nil`, `fields.resolvedScenarioTitle == nil`, `fields.resolvedToneTitle == nil`, `fields.scenarioTitle == "Networking"`, `fields.toneTitle == "Confident"`.
- **`helperReturnsStalledDirectionForFullyOffBrand0PercentStalledFixture`** — 0%-stalled own-scenario. Just-finished rep is the 4th rep in scenario A with `["shaky", "shaky", "shaky", "shaky"]`. Asserts: same shape as test 1 with `earlierRate == 0.0` and `recentRate == 0.0`.
- **`helperReturnsStalledDirectionForFullyOnBrand100PercentStalledFixture`** — 100%-stalled own-scenario. Just-finished rep is the 4th rep in scenario A with `["confident", "confident", "confident", "confident"]`. Asserts: same shape as test 1 with `earlierRate == 1.0` and `recentRate == 1.0`.

### Vision alignment

- **Coach-parity stage #4 (Adaptation) — "compare response across multiple attempts and either reinforce, vary, or replace the intervention with an explained rationale."** Per `docs/VISION.md`. Round 49 lifts the stalled-boundary contract from the surface layer to the helper layer: rounds 44 + 45 + 46 + 47 + 48 pin the surface outputs (prompt + deterministic note) across the full 2×2 direction × locus matrix; round 49 pins the helper-level direction classification at the boundary so a future regression that breaks ONLY the helper (without breaking the surface templates) is caught at the helper boundary, before either downstream surface silently swallows the change. The two-layer regression net (surface + helper) is now structurally complete on the stalled corner.
- **Coach-parity stage #3 (Intervention) — "prescribe a drill for a reason, name the observable target, and define what improvement would look like before the user starts."** Round 49 pins the helper-level contract the per-rep Intervention read depends on: at 0%-stalled the recommendation engine prescribes a real drill (the scenario is squarely below the bar), but the per-rep post-rep note must stay on metric-chain language — a coaching-shaped trajectory clause on a flat-line miss reads as the coach has nothing useful to say; at 100%-stalled the steady-state on-brand competence is not a coaching moment; at 50%-stalled the alternating arc has no real movement to read. The helper's `direction == .stalled` classification is what guarantees both surface gates close in lockstep, and round 49 pins it at the helper boundary at all three rate points.
- **Pillar #5 (Personalized coaching).** A coach (deterministic OR AI) who, after a stalled rep at any rate point, opens with a coaching-shaped trajectory clause reads as a coach who hasn't read the flat trajectory in front of them. Round 49 pins the helper-level direction classification that both surfaces depend on for their stalled gate, so a future regression that drifts the threshold or breaks the window-split arithmetic is caught at the helper output before either surface silently produces a non-stalled read on a stalled fixture.
- **Pillar #4 (Believable progress).** A stalled trajectory is the moment the post-rep note is most tempted to invent narrative drama. The helper's `direction == .stalled` classification is the single value both surfaces gate on; round 49 pins it at the helper boundary so the stalled gate is testably independent of the surface gates' implementation details.
- **Pillar #3 (Conversational intelligence).** The IM tone-drill TRAJECTORY clause (line ~1339 in `PostRepCoachNoteService.swift`) and SOLVED clause (line ~1348) both read from `PostRepCoachNoteInput.imToneDrillProgress` and `imToneDrillResolved`, which the finalizer threads through from `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)`. Round 49 makes the helper boundary's stalled-fixture output testably stable across all three rate-extreme fixtures.
- **Anti-overclaim.** No engine state mutation, no new freshness gate, no new constants, no schema bump, no migration, no production code change at all. The change is a no-op on the behavior side; the tests pin the helper-level stalled boundary that was already true at the primitive (`IMHistorySummary.toneDrillProgress`).
- **Engineering bans.** No placeholder logic. No dead toggles. No fragmented state. The three new tests READ only the existing `PracticeSession.id` + `PracticeSession.mode` + `PracticeSession.imConversationDetails.setup.scenario` + `PracticeSession.imConversationDetails.setup.targetTone` + `PracticeSession.imConversationDetails.actualTone` fields plus the existing `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)` helper output. No new storage, no schema bump, no migration, no new view inputs. The existing helper + primitive paths are preserved verbatim. Pure test addition on a pure-function surface.

### Branch + redesign-alignment notes

- All tracks land on `Redesign`, the redesign-lineage branch the rolling M24 deferred-slate work has been shipping on since round 11. The user brief explicitly calls this out: "ensure working on the redesign branch too (very important)." Round 49 preserves the round-by-round loop on the redesign lineage.
- Round 49 does not change any of the round-48 / round-47 / round-46 / ... / round-26 surfaces; every prior suite remains unchanged; the round-49 3 new `@Test` methods sit at the end of the existing `PostRepCoachNoteUserPromptAnchoringTests` suite under a new MARK section. The round-48-extended `PostRepCoachNoteUserPromptAnchoringTests` (24 tests, now 27 with round 49's additions), round-43 `PostRepCoachNoteJustFinishedRepAnchoringTests` (14 tests), round-42 `ToneDrillTrajectoryFreshnessTests` (15 tests), round-41 `ToneDrillSolvedFreshnessTests` (11 tests), and every other suite are preserved verbatim.
- The existing `PostRepCoachNoteToneTrajectoryTests` (5 tests, including the deterministic-path stalled fall-through which round 49's helper-boundary tests sit alongside) and `PostRepCoachNoteToneResolvedTests` are preserved verbatim — round 49 is purely additive on the test side, no fixture or production touch.

## Future moves

(Updated priority list — round 49 closed future move #30 from round 48's HANDOFF entirely, AND the round-48-boundary leg of future move #29 [the `earlierRate` and `recentRate` boundary assertions on the 0%-all-off and 100%-all-on fixtures, plus the 50%-alternating fixture]. The thin-A round-46 leg of #29 [`.progress == nil && .resolved == nil`] stays deferred. The rest roll forward, plus new moves from round 49.)

1. **Peer Sudden Death scores via `FriendsManager`.** Still blocked on `PublicProfileSnapshot` schema work.
2. **`coachNoteRevealed` cleanup.** Still risky — animation chain interleaving with celebration timing. Worth a dedicated refactor pass with proper visual QA (and a real device).
3. **Visual polish pass on the round-19 launch CTA.** Carried forward from rounds 19–48. Pure visual work, not destination logic.
4. **Visual polish pass on the round-20 SOLVED ribbon.** Carried forward from rounds 20–48. Pure visual work, not crossing logic.
5. **Day-rollover refresh for long-mounted observers.** Carried forward from round 22.
6. **Tier-change observation symmetry to other surfaces that read `AIRateLimiter.currentCap()` directly.** Carried forward from round 23.
7. **Refresh-on-rotate for the empty-state chip when the `CoachMemoryStore` mutates while AskNoumView is mounted.** Carried forward from round 25.
8. **Voice-tuned ack-chip glyphs.** Carried forward from round 26.
9. **Engine-level case-anchored amplification (post-round-37 escalation).** Carried forward from round 37 as a future move. Restraint pin: hold until real-conversation evidence calls for engine-level escalation.
10. **Engine-level under-repeated-pushback dampening (post-round-38 escalation).** Carried forward from round 38. Restraint pin: hold for real-conversation evidence.
11. **Engine-level case-anchored continuation (post-round-39 escalation).** Carried forward from round 39. Restraint pin: hold for real-conversation evidence.
12. **Engine-level under-pushback continuation dampening (post-round-40 escalation).** Carried forward from round 40. Restraint pin: hold for real-conversation evidence.
13. **Collapse the round-26 hypothesis-ack reflection in `coachCaseFormulationLines` into a single block with the round-32 rebuild-verdict lines when the predicate fires.** Carried forward from round 32. Hold for real-device QA.
14. **Trend-view distinction across the rebuild-verdict / case-state matrix surfaces.** Carried forward from rounds 30–48. Pure visual work; hold for real-device QA.
15. **Second-cycle ask register on the opener.** Note from round 35. Restraint pin: hold until real conversations show the cycle-agnostic ask reads as off.
16. **`CaseReviewCard` adaptation-log fuller history surface.** Note from round 36. Hold for real-device QA.
17. **`CaseReviewCard` case-anchored badge.** Note from round 37. Hold for real-device QA.
18. **`CaseReviewCard` under-pushback badge.** Note from round 38. Hold for real-device QA.
19. **`CaseReviewCard` case-anchored continuation badge.** Note from round 39. Hold for real-device QA.
20. **`CaseReviewCard` under-pushback continuation badge.** Note from round 40. Hold for real-device QA.
21. **Apply the freshness window to the `IMToneDrillProgress.stalled` chat-coach read separately from `recovering`/`slipping`.** Carried forward from round 42. Restraint pin: hold for real-conversation evidence of the stalled-and-stuck dynamic before splitting the window.
22. **Lift the recent-summaries + recent-proofs construction into `MomentumComputer` (or a sibling pure helper) for parallel anchoring tests.** Carried forward from round 43. Pure refactor; no behavior change; hold for a slow round.
23. **Defensive pin on `PostRepCoachNoteStore.record(_:)` dedupe-by-sessionID contract.** Carried forward from round 43. Hold for the right round.
24. **Unify the round-43 + round-44 fixture helpers (`imSession`, `nonIMSession`, `slippingScenarioSessions`, `crossingScenarioSessions`) into one private `IMHelpers` namespace shared across both suites.** Carried forward from round 44. Pure test refactor; no behavior change; hold for a slow round.
25. **AI-path tests for the metric branches (filler, score, pace) routed through `userPrompt(from:)`.** Carried forward from round 44. Hold for the right round.
26. **Cross-surface contract for the consecutive-clean-reps / personal-best / filler-trend momentum lines.** Note from round 45. Hold for the right round.
27. **End-to-end cross-surface contract that routes through `PracticeSessionFinalizer.recordPostRepCoachNote`.** Note from round 45. Higher complexity; hold for a build-host-equipped round.
28. **Mixed-just-finished thin + own-scenario crossing-elsewhere-in-history cross-surface contract.** Note from round 46. Hold for the right round.
29. **Helper-output assertion on the thin-A round-46 leg.** Partially carried over from round 48's move #29. Round 49 covered the round-47 50%-alternating leg and the round-48 boundary-fixture legs of #29. The remaining leg is `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:).progress == nil && .resolved == nil` for the thin-A justFinished (the round-46 thin-A fixture). A defensive test extending the round-49 helper-boundary pattern to the thin-A fixture would close the remaining leg of the original #29. Hold for the right round.
30. **Stalled-trajectory window-size = 2 vs window-size = 3 cross-surface contract.** Carried forward from round 48 as #31. The round-47/48/49 stalled fixtures all use 4 evaluated reps (`window = min(3, 4/2) = 2`). The next granularity is 6+ reps (`window = min(3, 6/2) = 3`), which exercises the wider-window stalled gate. A defensive cross-surface + helper-boundary test on a 6-rep stalled fixture (window=3) would extend the contract to the next window-size step. Round 49 makes the window-boundary regression more catchable (a regression that shifted the window-size bound from 2 to 3 on a 4-rep fixture would trip the round-49 `windowSize == 2` assertion), but the wider-window stalled gate on a 6-rep fixture is still uncovered. Hold for the right round.
31. **Stalled-trajectory at the boundary of `toneDrillProgressThreshold`.** Carried forward from round 48 as #32. The round-47/48/49 stalled fixtures have `delta == 0` (the cleanest stalled signal). A defensive cross-surface test at the exact boundary (`delta == ±toneDrillProgressThreshold - ε`) would lock the gate on its tightest discriminator. Hold for the right round.
32. **Stalled-A + parallel-thin-B cross-surface contract.** Carried forward from round 48 as #33. Round 47 covered stalled-A × (own / slipping-B / recovering-B); round 48 covered stalled-A × (own boundaries + prior-crossing-C). The remaining cross-scenario angle is stalled-A + thin-B (the parallel scenario has only 1-3 reps — no qualifying trajectory but also no SOLVED win). The helper threads only A through, so B's thinness is invisible to both surfaces — a symmetric companion to round 46's thin-A × parallel-loud tests. Defensive companion; hold for the right round.
33. **Helper-boundary assertion on the recovering / slipping / crossing fixtures.** New note from round 49. Round 49 covers the helper-boundary assertion for the three stalled fixtures (50%-alternating, 0%-all-off, 100%-all-on). The symmetric companion would assert the helper output for the round-44 recovering fixture (`.progress?.direction == .recovering`, exact rate values, `windowSize`), the round-44 slipping fixture (`.progress?.direction == .slipping`, exact rate values), and the round-45 crossing fixture (`.resolved != nil`, `resolvedScenarioTitle == "Difficult Conversation"`, the `earlierRate` / `recentRate` values on the crossing). Defensive belt extending the round-49 pattern to the non-stalled corners of the matrix. Hold for the right round.
34. **Helper-boundary assertion on the parallel-loud fixtures (slipping-B / recovering-B / prior-crossing-C / thin-B).** New note from round 49. The round-47/48 cross-surface tests assert the *surface outputs* don't name the parallel scenario when the just-finished rep is in A; a defensive helper-boundary test would assert the helper output (`MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)`) reads from scenario A's history only — `scenarioTitle == "Networking"`, never `"Difficult Conversation"` or `"Work Update"`, regardless of how loud the parallel scenario's history is. The round-43 `helperOnlyReadsJustFinishedScenarioForCrossingHistory` test covers the crossing case for the SOLVED branch; the symmetric companions for the trajectory branch are still uncovered. Hold for the right round.

## Build-host limitation (honest note for the next agent)

This environment has **no Xcode and no Swift toolchain**, so nothing in this round was compiled or run — not the app, not the test suite. The change is:

- Three new `@Test` methods (`helperReturnsStalledDirectionForAlternating50PercentStalledFixture`, `helperReturnsStalledDirectionForFullyOffBrand0PercentStalledFixture`, `helperReturnsStalledDirectionForFullyOnBrand100PercentStalledFixture`) appended to the existing `PostRepCoachNoteUserPromptAnchoringTests` suite in `NoumTests/NoumTests.swift`. Slotted immediately after the existing round-48 `userPromptAndDeterministicNoteFullyOnBrandStalledJustFinishedNeverProducesOwnTrajectoryClause` test and immediately before the suite's closing brace, under a new `// MARK: - Helper-level direction assertions on round-47/48 stalled fixtures (round 49)` section. The three stalled-A fixtures (50%-alternating in test 1, 0%-all-off in test 2, 100%-all-on in test 3) are built inline using the same shape the round-47/48 surface tests use.

All checks the next agent should run on a real build host:

1. `swift test --filter PostRepCoachNoteUserPromptAnchoringTests` — the suite now has 27 tests (round-44's 12 + round-45's 3 + round-46's 3 + round-47's 3 + round-48's 3 + round-49's 3); all should pass.
2. `swift test --filter PostRepCoachNoteJustFinishedRepAnchoringTests` — round-43's 14 tests should still pass verbatim (the round-49 additions are scoped to the round-44/45/46/47/48 suite).
3. `swift test --filter PostRepCoachNoteToneTrajectoryTests` — round-15's existing tests (including the deterministic-path stalled fall-through) should still pass verbatim.
4. `swift test --filter PostRepCoachNoteToneResolvedTests` — the existing SOLVED-branch tests should still pass verbatim.
5. `swift test --filter ToneDrillTrajectoryFreshnessTests` — round-42's 15 tests should still pass verbatim.
6. `swift test --filter ToneDrillSolvedFreshnessTests` — round-41's 11 tests should still pass verbatim.
7. `swift test --filter ToneDrillTrajectoryContextTests` — round-12's 8 tests should still pass.
8. `swift test --filter ToneDrillResolvedContextTests` — round-13's 4 tests should still pass.
9. `swift test --filter IMToneDrillSignalTests` — round-12/14's primitive tests should still pass.
10. `swift test --filter InterventionUnderPushbackContinuationTests` — round-40's 29 tests should still pass.
11. `swift test --filter CaseAnchoredContinuationTests` — round-39's 28 tests should still pass.
12. `swift test --filter CaseAnchoredAmplificationTests` — round-37's 27 tests should still pass.
13. `swift test --filter InterventionUnderRepeatedPushbackTests` — round-38's 32 tests should still pass.
14. `swift test --filter RebuildVerdictContextTests` — round-32's 25 tests should still pass.
15. `swift test --filter FreshRevisedReadContextTests` — round-31's 16 tests should still pass.
16. Confirm the round-21 SOLVED ribbon on the hero score card does NOT fire on any of the three round-49 stalled fixtures — same primitive (`toneDrillCrossing` → `toneDrillResolved`) routes both surfaces through the helper; round 49 asserts the helper's `resolved == nil` for each fixture so the two surfaces remain in lockstep with the ribbon.
17. Confirm the round-26 hypothesis-ack chip, round-32 rebuild-verdict line, round-37/38/39/40 case-state matrix, round-31 fresh-revised-read context block, round-41 SOLVED freshness window, round-42 TRAJECTORY freshness window, round-43 deterministic-path anchoring, round-44 AI-path anchoring, round-45 cross-surface SOLVED contract, round-46 cross-surface thin-just-finished contract, round-47 cross-surface stalled-just-finished contract, round-48 cross-surface stalled-just-finished boundary completion contract, and round-49 helper-level direction assertions on the round-47/48 stalled fixtures are all preserved verbatim.
