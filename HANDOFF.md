# HANDOFF — M24 deferred slate (round 46): TRAJECTORY + thin-just-finished cross-surface contract — both `PostRepCoachNoteService.userPrompt(from:)` AND `PostRepCoachNoteService.deterministicNote(input:)` stay silent on the loudest parallel scenario in the user's history when the just-finished rep is thin in its own scenario (no qualifying trajectory, no crossing). Round 46 closes future move #28 from round 45's HANDOFF ("Cross-surface contract test extending the round-45 SOLVED pattern to the TRAJECTORY + thin-just-finished case"). Pure test addition; no production code change, no engine state change, no schema bump, no migration, no new view inputs. The structural contract was already true at round 43 (the helper returns nil progress AND nil resolved when the just-finished rep's scenario has fewer than 4 evaluated reps in the session set) and was pinned per-surface at rounds 43 (`deterministicNoteNeverNamesAnotherScenarioWhenJustFinishedRepIsThin`) + 44 (`userPromptNeverNamesAnotherScenarioWhenJustFinishedRepIsThin`); round 46 locks the two surfaces in lockstep on the thin-just-finished branch with three new `@Test` methods on the existing `PostRepCoachNoteUserPromptAnchoringTests` suite so a future regression on EITHER surface (helper, prompt, deterministic note) trips the cross-surface contract independently of which surface broke first.

## Scope

Round 45 closed the SOLVED-branch cross-surface contract on three angles (`userPromptAndDeterministicNoteSolvedBranchAnchorOnJustFinishedScenario` baseline + `…HoldsWhenParallelScenarioIsRecentlySlipping` defensive + `…NeverNamesParallelPriorCrossing` defensive), matching round 44's single TRAJECTORY-branch cross-surface test (`userPromptAndDeterministicNoteNameSameJustFinishedRepsScenario`). The two together pin both surfaces in lockstep on the **happy paths** (recovering-A or crossing-A in the just-finished rep's scenario).

Round 45 left open the **thin** path: a just-finished rep with no qualifying trajectory (one rep in scenario A, no history) is the most coaching-tempting cross-rep cold open — the just-finished rep is silent, but the rest of the user's session set may carry the loudest open trajectory (slipping somewhere, recovering somewhere) or the most coaching-significant past win (a prior crossing in another scenario). Future move #28 noted: "a symmetric companion would test thin just-finished rep in A (no qualifying trajectory) + slipping-B (loud open trajectory) — both surfaces stay silent on B even though it's the only qualifying trajectory in the session set. The existing single-surface tests cover this per-surface; the cross-surface lockstep test is the deferred move."

Round 46 closes that gap. Three new `@Test` methods on `PostRepCoachNoteUserPromptAnchoringTests`, slotted at the end of the suite immediately after `userPromptAndDeterministicNoteSolvedBranchNeverNamesParallelPriorCrossing`, lock the TRAJECTORY + thin-just-finished cross-surface contract on three angles:

1. **Baseline parallel-slipping** (`userPromptAndDeterministicNoteThinJustFinishedNeverNameParallelSlippingScenario`) — just-finished rep is the lone evaluated rep in scenario A (Networking, confident); scenario B (Difficult Conversation) carries a 4-rep below-bar slip — the loudest open trajectory in the session set and the recommendation engine's next prescription. Both surfaces stay silent on B; the `tone-match` clause never fires because the helper's `imToneDrillProgress` for scenario A is nil.
2. **Parallel-recovering defensive** (`userPromptAndDeterministicNoteThinJustFinishedNeverNameParallelRecoveringScenario`) — same thin-A just-finished + scenario B carries a 4-rep recovering trajectory (the "you're recovering elsewhere" pivot would be the loudest open positive thread). The helper threads scenario A only through `toneDrillProgress`, so B's recovery is invisible to both surfaces.
3. **Parallel-prior-crossing defensive** (`userPromptAndDeterministicNoteThinJustFinishedNeverNameParallelPriorCrossing`) — same thin-A just-finished + scenario C (Work Update, professional) had a prior crossing rep at -20 days. The "you've done it before in another scenario, here's a fresh thin rep elsewhere — pivot to C's old win" cold-open is structurally impossible: the helper threads ONLY scenario A through `toneDrillCrossing`, so C's old win is invisible to both surfaces.

The three tests together pin both surfaces in lockstep on the thin-just-finished branch the same way round 44's single test and round 45's three tests do for the recovering / crossing branches: a future regression that lets EITHER surface (helper, prompt, or deterministic note) name a non-just-finished scenario when the just-finished rep is thin trips at least one of the three round-46 tests independently of which surface broke first.

Defensive scoping (intentional restraint, same shape as rounds 41/42/43/44/45):

- **Pure test addition, no production code change.** Round 46 modifies only `NoumTests/NoumTests.swift` and `HANDOFF.md`. Every production surface (`MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)`, `PostRepCoachNoteService.userPrompt(from:)`, `PostRepCoachNoteService.deterministicNote(input:)`, `PostRepCoachNoteService.metricSentence(for:persona:)`, `PostRepCoachNoteService.imToneResolvedSentence(...)`, `PracticeSessionFinalizer.recordPostRepCoachNote`, every IM tone-drill / SOLVED branch) is preserved verbatim. The structural contract round 46 pins was already true at round 43 (helper anchoring) and pinned per-surface at rounds 43 + 44; round 46 locks the two surfaces in lockstep on the thin-just-finished branch.
- **No new freshness gate.** Same rationale as rounds 43/44/45: the post-rep note is written EXACTLY ONCE, immediately after the rep finalizes, against signals computed strictly for the just-finished rep's scenario. The per-rep anchoring IS the freshness contract on both the deterministic and AI paths. Round 46 does NOT introduce a recency window.
- **No engine state change.** `IMHistorySummary.toneDrillProgress(from:scenario:)` and `IMHistorySummary.toneDrillCrossing(in:scenario:currentRepId:)` are unchanged. The `MomentumComputer.imToneDrillNoteFields` helper is unchanged. Both `PostRepCoachNoteService.userPrompt(from:)` and `PostRepCoachNoteService.deterministicNote(input:)` bodies are unchanged. No new persisted state, no schema bump, no migration, no new view inputs.
- **Both surfaces are `nonisolated static`.** Round 46 drives both the AI-path and deterministic-path surfaces directly without AI provider stubs — the same testability levers rounds 43 + 44 + 45 used. No mock provider, no async wiring, no rate-limit gymnastics.
- **Test fixtures mirror round 43's + round 44's + round 45's verbatim.** `imSession(id:scenario:targetTone:actualTone:daysOffset:)`, `slippingScenarioSessions(...)`, `crossingScenarioSessions(...)`, and the `makeInput(justFinished:allSessions:score:)` factory — same shapes the round-44/45 suite already declares; the round-46 tests are net new methods on the existing suite, reusing the existing fixtures without duplicating them. The parallel-recovering fixture in test 2 is built inline (same shape as the round-43 `recoveringA` inline fixture), since the round-44/45 suite never abstracted "recovering" into a helper — the inline 4-rep two-off-two-on shape stays readable and self-contained. (Future move #24 — unifying the round-43 + round-44 helpers into a shared namespace — stays open.)
- **Tests pin BOTH directions of the cross-rep cold-open structural impossibility on the TRAJECTORY + thin-just-finished branch.** Slipping-B parallel, recovering-B parallel, and prior-crossing-C parallel — three angles, all gated on the same `imToneDrillProgress`/`imToneDrillResolved` fields being nil for the just-finished rep, all pinned by end-to-end tests routed through the helper-then-prompt-and-note wire.
- **Each test asserts BOTH surfaces in one method.** A future regression that breaks anchoring on either surface trips the relevant test independently of which surface broke first — the cross-surface lockstep is the round-46 contribution. The existing round-43 `deterministicNoteNeverNamesAnotherScenarioWhenJustFinishedRepIsThin` and round-44 `userPromptNeverNamesAnotherScenarioWhenJustFinishedRepIsThin` continue to fail-fast in isolation if their single surface regresses; the round-46 tests add the cross-surface coupling.

User brief, unchanged round to round: "continue from the existing TO-DO, ensure working towards getting the app towards the vision plan, and all round A+, make my dream I had come true too, ensure working on the redesign branch too (very important)."

Translation, this round:

- Three new `@Test` methods (`userPromptAndDeterministicNoteThinJustFinishedNeverNameParallelSlippingScenario`, `userPromptAndDeterministicNoteThinJustFinishedNeverNameParallelRecoveringScenario`, `userPromptAndDeterministicNoteThinJustFinishedNeverNameParallelPriorCrossing`) appended to the existing `PostRepCoachNoteUserPromptAnchoringTests` suite, slotted immediately after `userPromptAndDeterministicNoteSolvedBranchNeverNamesParallelPriorCrossing` so the recovering / crossing / thin cross-surface contracts read as one contiguous block. No new fixtures.
- The redesign-branch invariant: round 46 is a `Redesign`-branch push per the user brief. It preserves the round-by-round loop on the redesign lineage that has been the home of rounds 11–45.

## What shipped

### Track 1 — Three TRAJECTORY + thin-just-finished cross-surface contract tests (`NoumTests/NoumTests.swift`)

Three new `@Test` methods on the existing `struct PostRepCoachNoteUserPromptAnchoringTests` suite (the round-44/45 suite, now 18 tests), slotted under a new `// MARK: - Cross-surface contract: thin just-finished + loud parallel scenario (round 46)` section immediately after the existing round-45 `userPromptAndDeterministicNoteSolvedBranchNeverNamesParallelPriorCrossing` test and immediately before the closing brace of the suite. The parallel-recovering fixture in test 2 is built inline (4-rep two-off-two-on shape, same as the round-43 `recoveringA` inline fixture); the parallel-slipping fixture in test 1 and the parallel-prior-crossing fixture in test 3 reuse the existing `slippingScenarioSessions` and `crossingScenarioSessions` helpers verbatim.

- **`userPromptAndDeterministicNoteThinJustFinishedNeverNameParallelSlippingScenario`** — Baseline thin-just-finished cross-surface contract. Just-finished rep is the lone evaluated rep in scenario A (Networking, confident, on-brand); scenario B (Difficult Conversation, calm) has a 4-rep below-bar slip. Asserts: prompt does NOT contain `"tone-match"`, `"SOLVED this rep"`, `"Difficult Conversation"`, lowercased `"difficult conversation"`. Note (lowercased) does NOT contain `"difficult conversation"` or `"calm tone"`. The helper returns nil progress AND nil resolved for the thin-A justFinished, so neither tone-drill clause fires on either surface; the metric chain takes over on the deterministic path and the AI prompt's MOMENTUM block omits the tone-drill lines.
- **`userPromptAndDeterministicNoteThinJustFinishedNeverNameParallelRecoveringScenario`** — Defensive: same thin-A just-finished + 4-rep recovering trajectory in scenario B (the "you're recovering elsewhere" pivot would be the loudest open positive thread the recommendation engine could surface). Both surfaces remain silent on B; the helper threads only scenario A through `toneDrillProgress`. Asserts: same `tone-match` + `SOLVED this rep` + `"Difficult Conversation"` + `"Calm tone in Difficult Conversation"` absence on prompt; same `"difficult conversation"` + `"calm tone"` absence on note (lowercased).
- **`userPromptAndDeterministicNoteThinJustFinishedNeverNameParallelPriorCrossing`** — Defensive: same thin-A just-finished + scenario C (Work Update, professional) prior crossing at -20 days. The "you've done it before in another scenario" cold-open is structurally impossible. Asserts: prompt does NOT contain `"tone-match"`, `"SOLVED this rep"`, `"Work Update"`, `"Professional"`, lowercased `"work update"`. Note (lowercased) does NOT contain `"work update"` or `"professional"`.

### Vision alignment

- **Coach-parity stage #4 (Adaptation) — "compare response across multiple attempts and either reinforce, vary, or replace the intervention with an explained rationale."** Per `docs/VISION.md`. Round 46 closes the third of three structural angles on the per-rep Adaptation read: rounds 44 + 45 pin the loud paths (recovering trajectory, crossing rep); round 46 pins the quiet path (thin just-finished). The chat coach's Adaptation read (rounds 41/42 surface, freshness-gated), the post-rep coach note's deterministic-path Adaptation read (round 43 surface), the post-rep coach note's AI-path Adaptation read (round 44 surface), the SOLVED-branch cross-surface contract (round 45), AND now the TRAJECTORY + thin-just-finished cross-surface contract (round 46) all have testable structural anchoring contracts. The 2x2 cross-surface matrix (recovering / crossing × loud / silent just-finished) is now structurally pinned in lockstep.
- **Coach-parity stage #3 (Intervention) — "prescribe a drill for a reason, name the observable target, and define what improvement would look like before the user starts."** Round 46 pins the most coaching-tempting failure mode: the just-finished rep is silent on its own scenario, but a loud open trajectory or past win exists elsewhere. A real coach who pivots to "let me talk about your other scenario instead" is no longer paying attention to the rep the user just produced. Round 46 makes that pivot structurally impossible on both surfaces in lockstep.
- **Pillar #5 (Personalized coaching).** A coach (deterministic OR AI) who, after a fresh thin rep in scenario A, opens with "your calm tone in Difficult Conversation is slipping" or "you're recovering in Networking" or "you've already solved Work Update" reads as a coach who hasn't read the rep the user just sent. Round 46 makes the per-rep anchoring structurally guaranteed on both paths in lockstep even when the just-finished rep is the quietest signal in the session set. The user can't tell which path produced their note (the surface labels it `isAIBacked: true/false`), so the thin-anchoring contract must hold on both — and now is pinned by tests that drive both surfaces simultaneously.
- **Pillar #4 (Believable progress).** A thin just-finished rep is the moment the post-rep note has the least to say. The temptation to pull from elsewhere in the session set ("you've done it before in another scenario!") is exactly the failure mode a real coach avoids. Round 46 makes that fall-back impossible on both surfaces in one method, so a future regression on either path is caught before it ships to the user.
- **Pillar #3 (Conversational intelligence).** The AI prompt's metric chain (filler, score, pace, etc.) is preserved verbatim. The IM tone-drill TRAJECTORY clause (line ~1339 in `PostRepCoachNoteService.swift`) and SOLVED clause (line ~1348) both read from the `imToneDrill*` input fields the round-43 helper anchors, and the deterministic note's branch 0 (line ~619) reads from the same wire. Round 46 just makes the two surfaces' thin-just-finished reads testably inseparable.
- **Anti-overclaim.** No engine state mutation, no new freshness gate, no new constants, no schema bump, no migration, no production code change at all. The change is a no-op on the behavior side; the tests pin the cross-surface structural contract that was already true.
- **Engineering bans.** No placeholder logic. No dead toggles. No fragmented state. The three new tests READ only the existing `PracticeSession.id` + `PracticeSession.mode` + `PracticeSession.imConversationDetails.setup.scenario` + `PracticeSession.imConversationDetails.setup.targetTone` fields plus the existing `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)` helper output (via the round-44/45 `makeInput` factory). No new storage, no schema bump, no migration, no new view inputs. The existing `PostRepCoachNoteService.userPrompt(from:)`, `PostRepCoachNoteService.deterministicNote(input:)`, `PostRepCoachNoteService.metricSentence(for:persona:)`, `PostRepCoachNoteService.imToneResolvedSentence(...)`, and `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)` paths are preserved verbatim. Pure test addition on pure-function surfaces.

### Branch + redesign-alignment notes

- All tracks land on `Redesign`, the redesign-lineage branch the rolling M24 deferred-slate work has been shipping on since round 11. The user brief explicitly calls this out: "ensure working on the redesign branch too (very important)." Round 46 preserves the round-by-round loop on the redesign lineage.
- Round 46 does not change any of the round-45 / round-44 / round-43 / round-42 / round-41 / round-40 / ... / round-26 surfaces; every prior suite remains unchanged; the round-46 3 new `@Test` methods sit at the end of the existing `PostRepCoachNoteUserPromptAnchoringTests` suite (the round-44/45 suite). The round-45 `PostRepCoachNoteUserPromptAnchoringTests` (15 tests, now 18 with round 46's additions), round-43 `PostRepCoachNoteJustFinishedRepAnchoringTests` (14 tests), round-42 `ToneDrillTrajectoryFreshnessTests` (15 tests), round-41 `ToneDrillSolvedFreshnessTests` (11 tests), and every other suite are preserved verbatim.
- The existing `PostRepCoachNoteToneTrajectoryTests` (5 tests) and `PostRepCoachNoteToneResolvedTests` are preserved verbatim — round 46 is purely additive on the test side, no fixture or production touch.

## Future moves

(Updated priority list — round 46 closed the TRAJECTORY + thin-just-finished cross-surface contract, the deferred move #28 from round 45. The rest roll forward.)

1. **Peer Sudden Death scores via `FriendsManager`.** Still blocked on `PublicProfileSnapshot` schema work.
2. **`coachNoteRevealed` cleanup.** Still risky — animation chain interleaving with celebration timing. Worth a dedicated refactor pass with proper visual QA (and a real device).
3. **Visual polish pass on the round-19 launch CTA.** Carried forward from rounds 19–45. Pure visual work, not destination logic.
4. **Visual polish pass on the round-20 SOLVED ribbon.** Carried forward from rounds 20–45. Pure visual work, not crossing logic.
5. **Day-rollover refresh for long-mounted observers.** Carried forward from round 22.
6. **Tier-change observation symmetry to other surfaces that read `AIRateLimiter.currentCap()` directly.** Carried forward from round 23.
7. **Refresh-on-rotate for the empty-state chip when the `CoachMemoryStore` mutates while AskNoumView is mounted.** Carried forward from round 25.
8. **Voice-tuned ack-chip glyphs.** Carried forward from round 26.
9. **Engine-level case-anchored amplification (post-round-37 escalation).** Carried forward from round 37 as a future move. Restraint pin: hold until real-conversation evidence calls for engine-level escalation.
10. **Engine-level under-repeated-pushback dampening (post-round-38 escalation).** Carried forward from round 38. Restraint pin: hold for real-conversation evidence.
11. **Engine-level case-anchored continuation (post-round-39 escalation).** Carried forward from round 39. Restraint pin: hold for real-conversation evidence.
12. **Engine-level under-pushback continuation dampening (post-round-40 escalation).** Carried forward from round 40. Restraint pin: hold for real-conversation evidence.
13. **Collapse the round-26 hypothesis-ack reflection in `coachCaseFormulationLines` into a single block with the round-32 rebuild-verdict lines when the predicate fires.** Carried forward from round 32. Hold for real-device QA.
14. **Trend-view distinction across the rebuild-verdict / case-state matrix surfaces.** Carried forward from rounds 30–45. Pure visual work; hold for real-device QA.
15. **Second-cycle ask register on the opener.** Note from round 35. Restraint pin: hold until real conversations show the cycle-agnostic ask reads as off.
16. **`CaseReviewCard` adaptation-log fuller history surface.** Note from round 36. Hold for real-device QA.
17. **`CaseReviewCard` case-anchored badge.** Note from round 37. Hold for real-device QA.
18. **`CaseReviewCard` under-pushback badge.** Note from round 38. Hold for real-device QA.
19. **`CaseReviewCard` case-anchored continuation badge.** Note from round 39. Hold for real-device QA.
20. **`CaseReviewCard` under-pushback continuation badge.** Note from round 40. Hold for real-device QA.
21. **Apply the freshness window to the `IMToneDrillProgress.stalled` chat-coach read separately from `recovering`/`slipping`.** Carried forward from round 42. Restraint pin: hold for real-conversation evidence of the stalled-and-stuck dynamic before splitting the window.
22. **Lift the recent-summaries + recent-proofs construction into `MomentumComputer` (or a sibling pure helper) for parallel anchoring tests.** Carried forward from round 43. The finalizer's `recentSessionSummaries` and `recentProofQuotes` are constructed inline in `recordPostRepCoachNote` (the same shape as the pre-round-43 IM tone-drill block). They DON'T have the same cross-rep cold-open concern (they're explicitly cross-rep summaries by design), but lifting them into pure helpers would make the per-rep finalizer wire more uniformly testable. Pure refactor; no behavior change; hold for a slow round.
23. **Defensive pin on `PostRepCoachNoteStore.record(_:)` dedupe-by-sessionID contract.** Carried forward from round 43. The store de-duplicates notes by sessionID (the finalize → AI upgrade → re-record path relies on this); a defensive store-level test pinning the dedupe contract and the "never-write-for-deleted-session" guard would lock the surface symmetric to the round-43 helper. Hold for the right round.
24. **Unify the round-43 + round-44 fixture helpers (`imSession`, `nonIMSession`, `slippingScenarioSessions`, `crossingScenarioSessions`) into one private `IMHelpers` namespace shared across both suites.** Carried forward from round 44. The round-44 suite redeclares all four helpers verbatim from round 43 for self-containment; the round-45 + round-46 tests reuse the round-44 helpers but a future refactor lifts them into a shared namespace so a future test author doesn't need to keep four helpers in sync across the trajectory + deterministic-anchoring + AI-anchoring + cross-surface + resolved suites. Pure test refactor; no behavior change; hold for a slow round. Round 46 adds a 5th candidate to the namespace: a `recoveringScenarioSessions(scenario:targetTone:offBrandTone:onBrandTone:earliestDayOffset:)` helper currently built inline in test 2.
25. **AI-path tests for the metric branches (filler, score, pace) routed through `userPrompt(from:)`.** Carried forward from round 44. The round-43 + round-44 + round-45 + round-46 contract is locked on the IM tone-drill clauses; the per-rep metric branches (filler comparison vs baseline, zero-fillers, score bands, etc.) construct prompt lines from the same `PostRepCoachNoteInput` fields. A defensive suite mirroring round-44's shape (12 tests, fixture-driven, route through `userPrompt(from:)`) would extend the same anchoring contract surface to the metric branches. Hold for the right round.
26. **Cross-surface contract for the consecutive-clean-reps / personal-best / filler-trend momentum lines.** Note from round 45. The same `userPromptAndDeterministicNote*` pattern that locks the IM tone-drill clauses on both surfaces could extend to the non-IM momentum lines — consecutive clean reps (line ~1322 in `userPrompt`), personal best (~1325), filler trend (~1330), score trend (~1333), weekly rep count (~1336). The deterministic priority chain reads these in branches 0a–0d (line ~627-662 in `metricSentence`). A symmetric cross-surface suite (one test per momentum branch) would pin both paths in lockstep. Hold for the right round.
27. **End-to-end cross-surface contract that routes through `PracticeSessionFinalizer.recordPostRepCoachNote`.** Note from round 45. The current cross-surface contracts (round 44 TRAJECTORY + round 45 SOLVED + round 46 thin-just-finished) route through the pure `userPrompt(from:)` + `deterministicNote(input:)` surfaces directly, bypassing the finalizer wire. A defensive end-to-end test that constructs a `PracticeSession` set, invokes the finalizer, and asserts the persisted `PostRepCoachNote.noteText` headlines the just-finished rep's scenario (or stays silent when thin) would lock the FULL wire (finalizer → helper → input → deterministic-or-AI note). Higher complexity (the finalizer takes a `CoachMemoryStore`, `PracticeSessionsStore`, etc.); hold for a build-host-equipped round.
28. **Stalled-trajectory cross-surface contract.** New note from round 46. The round-44 / round-45 / round-46 cross-surface contracts cover recovering / crossing / thin trajectories. The `.stalled` direction (delta below ±15% threshold) is gated by the prompt + deterministic note independently (`progress.direction != .stalled` at line 1340 in `userPrompt`, separate gate in `metricSentence`). A defensive cross-surface test pinning the stalled gate in lockstep (both surfaces silent on a stalled-A trajectory, AND silent on parallel-B when A is stalled) would close the 2x2 direction × locus matrix. Hold for a slow round.
29. **Mixed-just-finished thin + own-scenario crossing-elsewhere-in-history cross-surface contract.** New note from round 46. The trickiest cold-open is: just-finished rep is thin (one rep in A), but the SAME scenario A had a CROSSING rep weeks ago that's now stale. The helper's `toneDrillCrossing(in:scenario:currentRepId:)` is anchored on `currentRepId`, so an old crossing in the same scenario but on a different rep is invisible to the just-finished rep's read — by construction. A defensive cross-surface test would pin this on both surfaces. Hold for the right round.
30. **Helper-output assertion layered into the cross-surface tests.** New note from round 46. The round-46 baseline test asserts the *prompt + note text* don't name the parallel scenario, which is the user-facing contract. A defensive belt would also assert at the helper output: `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:).progress == nil && .resolved == nil` for the thin-A justFinished. The single-surface round-43 test `helperReturnsNilProgressOnThinJustFinishedRep` covers this on the deterministic suite; lifting it into the cross-surface suite would catch a helper regression independent of the surface tests. Hold for the right round.

## Build-host limitation (honest note for the next agent)

This environment has **no Xcode and no Swift toolchain**, so nothing in this round was compiled or run — not the app, not the test suite. The change is:

- Three new `@Test` methods (`userPromptAndDeterministicNoteThinJustFinishedNeverNameParallelSlippingScenario`, `userPromptAndDeterministicNoteThinJustFinishedNeverNameParallelRecoveringScenario`, `userPromptAndDeterministicNoteThinJustFinishedNeverNameParallelPriorCrossing`) appended to the existing `PostRepCoachNoteUserPromptAnchoringTests` suite in `NoumTests/NoumTests.swift`. Slotted immediately after the existing round-45 `userPromptAndDeterministicNoteSolvedBranchNeverNamesParallelPriorCrossing` test and immediately before the suite's closing brace. The parallel-recovering fixture in test 2 is built inline (4-rep two-off-two-on shape, same as the round-43 `recoveringA` inline fixture); the parallel-slipping and parallel-prior-crossing fixtures in tests 1 and 3 reuse the existing `slippingScenarioSessions` and `crossingScenarioSessions` helpers verbatim.

All checks the next agent should run on a real build host:

1. `swift test --filter PostRepCoachNoteUserPromptAnchoringTests` — the suite now has 18 tests (round-44's 12 + round-45's 3 + round-46's 3); all should pass.
2. `swift test --filter PostRepCoachNoteJustFinishedRepAnchoringTests` — round-43's 14 tests should still pass verbatim (the round-46 additions are scoped to the round-44/45 suite).
3. `swift test --filter PostRepCoachNoteToneTrajectoryTests` — round-15's existing tests should still pass verbatim.
4. `swift test --filter PostRepCoachNoteToneResolvedTests` — the existing SOLVED-branch tests should still pass verbatim.
5. `swift test --filter ToneDrillTrajectoryFreshnessTests` — round-42's 15 tests should still pass verbatim.
6. `swift test --filter ToneDrillSolvedFreshnessTests` — round-41's 11 tests should still pass verbatim.
7. `swift test --filter ToneDrillTrajectoryContextTests` — round-12's 8 tests should still pass.
8. `swift test --filter ToneDrillResolvedContextTests` — round-13's 4 tests should still pass.
9. `swift test --filter IMToneDrillSignalTests` — round-12/14's primitive tests should still pass.
10. `swift test --filter InterventionUnderPushbackContinuationTests` — round-40's 29 tests should still pass.
11. `swift test --filter CaseAnchoredContinuationTests` — round-39's 28 tests should still pass.
12. `swift test --filter CaseAnchoredAmplificationTests` — round-37's 27 tests should still pass.
13. `swift test --filter InterventionUnderRepeatedPushbackTests` — round-38's 32 tests should still pass.
14. `swift test --filter RebuildVerdictContextTests` — round-32's 25 tests should still pass.
15. `swift test --filter FreshRevisedReadContextTests` — round-31's 16 tests should still pass.
16. Boot the app on simulator, finish an IM rep in scenario A (e.g. Networking, confident) that is THIN — the just-finished rep is the lone evaluated rep in A, with NO qualifying history — while the user ALSO has parallel signals in another scenario (e.g. a recent 4-rep below-bar slip in Difficult Conversation, or a 4-rep recovering trajectory in Difficult Conversation, or an old crossing rep in Work Update from weeks ago). Confirm the post-rep Coach's Read card on the Summary screen speaks to scenario A's per-rep metrics (filler, score, pace, weekly rep count) — NEVER mentions the parallel scenario's title in the IM tone-drill block. The card should fall through to a metric-chain read, not a tone-drill clause referring to another scenario.
17. With the AI provider configured (when available), repeat the test in step 16 and confirm the AI-backed note also stays on scenario A's metrics and never names the parallel scenario in the SOLVED or TRAJECTORY clause. The AI path's structural impossibility of a thin-A cross-rep cold open is now both architecturally enforced AND tested at the prompt layer.
18. Confirm the round-21 SOLVED ribbon on the hero score card does NOT fire on a thin just-finished rep in A (no crossing in the just-finished rep's scenario) even when another scenario in history has a crossing — same primitive (`toneDrillCrossing`) routes both surfaces through the helper, so the two surfaces remain in lockstep with the ribbon.
19. Confirm the round-26 hypothesis-ack chip, round-32 rebuild-verdict line, round-37/38/39/40 case-state matrix, round-31 fresh-revised-read context block, round-41 SOLVED freshness window, round-42 TRAJECTORY freshness window, round-43 deterministic-path anchoring, round-44 AI-path anchoring, round-45 cross-surface SOLVED contract, and round-46 cross-surface thin-just-finished contract are all preserved verbatim.
