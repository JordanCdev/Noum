# HANDOFF — M24 deferred slate (round 45): SOLVED-branch cross-surface contract — both `PostRepCoachNoteService.userPrompt(from:)` AND `PostRepCoachNoteService.deterministicNote(input:)` headline the just-finished rep's scenario in the SOLVED clause, never any parallel scenario in the user's history. Round 45 closes future move #25 from round 44's HANDOFF ("Cross-surface contract test extending the `userPromptAndDeterministicNoteNameSameJustFinishedRepsScenario` pattern to the SOLVED branch"). Pure test addition; no production code change, no engine state change, no schema bump, no migration, no new view inputs. The structural contract was already true at round 43 (the resolved trio is anchored on the just-finished rep's scenario by the round-43 helper) and was pinned per-surface at rounds 43 + 44; round 45 locks the two surfaces in lockstep with a 3-test extension to `PostRepCoachNoteUserPromptAnchoringTests` so a future regression on EITHER surface (helper, prompt, deterministic note) trips the cross-surface contract independently of which surface broke first.

## Scope

Round 44 added the AI-path symmetric pin (`PostRepCoachNoteUserPromptAnchoringTests`, 12 tests) and a single TRAJECTORY-branch cross-surface contract test (`userPromptAndDeterministicNoteNameSameJustFinishedRepsScenario`) that calls BOTH the AI-prompt and deterministic-note surfaces on the same recovering-A-and-slipping-B fixture and asserts both name scenario A and neither names scenario B. Round 44 left the SOLVED-branch cross-surface contract open (future move #25 in round 44's HANDOFF): the two SOLVED branches were already tested in isolation per surface (round 43's `deterministicNoteHeadlinesResolvedWinOnlyForJustFinishedRepsScenario` on the deterministic path + round 44's `userPromptNeverNamesAnotherScenarioOnSolvedCrossSurfaceContract` and `userPromptSolvedClauseNamesJustFinishedScenarioOnly` on the AI path), but no test routes BOTH surfaces through the same SOLVED fixture and asserts they speak to the same scenario.

Round 45 closes that gap. Three new `@Test` methods on `PostRepCoachNoteUserPromptAnchoringTests`, slotted at the end of the suite immediately after `userPromptAndDeterministicNoteNameSameJustFinishedRepsScenario`, lock the SOLVED-branch cross-surface contract on three angles:

1. **Baseline SOLVED cross-surface** (`userPromptAndDeterministicNoteSolvedBranchAnchorOnJustFinishedScenario`) — just-finished rep IS the crossing rep in scenario A (Difficult Conversation, calm); scenario B (Networking, confident) sits in the session set as a thin parallel (one rep, no qualifying history). Both surfaces name scenario A's title in the SOLVED clause; neither names scenario B. Symmetric to round 44's TRAJECTORY-branch cross-surface test.
2. **Parallel-currently-slipping defensive** (`userPromptAndDeterministicNoteSolvedBranchHoldsWhenParallelScenarioIsRecentlySlipping`) — same crossing-A fixture + a current 4-rep below-bar slip in scenario B (the recommendation engine would prescribe B's drill next, but it's invisible to the post-rep note's per-rep helper). Both surfaces remain on scenario A's SOLVED win.
3. **Parallel-prior-crossing defensive** (`userPromptAndDeterministicNoteSolvedBranchNeverNamesParallelPriorCrossing`) — same crossing-A fixture + scenario C (Work Update, professional) had a prior crossing rep at earlier date (-20 days). The "you've already done it before" pivot is structurally impossible: the helper threads ONLY scenario A through `toneDrillCrossing(in:scenario:currentRepId:)`, so C's old win is invisible to both surfaces.

The three tests together pin the two surfaces in lockstep on the SOLVED branch the same way round 44's single TRAJECTORY-branch test does for the trajectory branch: a future regression that lets EITHER surface (helper, prompt, or deterministic note) name a non-just-finished scenario in the SOLVED clause trips at least one of the three round-45 tests independently of which surface broke first.

Defensive scoping (intentional restraint, same shape as rounds 41/42/43/44):

- **Pure test addition, no production code change.** Round 45 modifies only `NoumTests/NoumTests.swift`. Every production surface (`MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)`, `PostRepCoachNoteService.userPrompt(from:)`, `PostRepCoachNoteService.deterministicNote(input:)`, `PostRepCoachNoteService.metricSentence(for:persona:)`, `PostRepCoachNoteService.imToneResolvedSentence(...)`, `PracticeSessionFinalizer.recordPostRepCoachNote`, every IM tone-drill / SOLVED branch) is preserved verbatim. The structural contract round 45 pins was already true at round 43 (helper anchoring), pinned per-surface at round 43 (deterministic) and round 44 (AI prompt); round 45 locks the two surfaces in lockstep on the SOLVED branch.
- **No new freshness gate.** Same rationale as rounds 43/44: the post-rep note is written EXACTLY ONCE, immediately after the rep finalizes, against signals computed strictly for the just-finished rep's scenario. The per-rep anchoring IS the freshness contract on both the deterministic and AI paths. Round 45 does NOT introduce a recency window.
- **No engine state change.** `IMHistorySummary.toneDrillProgress(from:scenario:)` and `IMHistorySummary.toneDrillCrossing(in:scenario:currentRepId:)` are unchanged. The `MomentumComputer.imToneDrillNoteFields` helper is unchanged. Both `PostRepCoachNoteService.userPrompt(from:)` and `PostRepCoachNoteService.deterministicNote(input:)` bodies are unchanged. No new persisted state, no schema bump, no migration, no new view inputs.
- **Both surfaces are `nonisolated static`.** Round 45 drives both the AI-path and deterministic-path surfaces directly without AI provider stubs — the same testability levers rounds 43 + 44 used. No mock provider, no async wiring, no rate-limit gymnastics.
- **Test fixtures mirror round 43's + round 44's verbatim.** `imSession(id:scenario:targetTone:actualTone:daysOffset:)`, `slippingScenarioSessions(...)`, `crossingScenarioSessions(...)`, and the `makeInput(justFinished:allSessions:score:)` factory — same shapes the round-44 suite already declares; the round-45 tests are net new methods on the existing suite, reusing the existing fixtures without duplicating them. (Future move #24 — unifying the round-43 + round-44 helpers into a shared namespace — stays open; round 45's restraint is to add tests on the existing suite without restructuring the fixture layout.)
- **Tests pin BOTH directions of the cross-rep cold-open structural impossibility on the SOLVED branch.** Thin-B parallel, currently-slipping-B parallel, and prior-crossing-C parallel — three angles, all gated on the same `imToneDrillResolved*` fields, all pinned by end-to-end tests routed through the helper-then-prompt-and-note wire.
- **Each test asserts BOTH surfaces in one method.** A future regression that breaks anchoring on either surface trips the relevant test independently of which surface broke first — the cross-surface lockstep is the round-45 contribution. The existing round-43 `deterministicNoteHeadlinesResolvedWinOnlyForJustFinishedRepsScenario` and round-44 `userPromptNeverNamesAnotherScenarioOnSolvedCrossSurfaceContract` continue to fail-fast in isolation if their single surface regresses; the round-45 tests add the cross-surface coupling.

User brief, unchanged round to round: "continue from the existing TO-DO, ensure working towards getting the app towards the vision plan, and all round A+, make my dream I had come true too, ensure working on the redesign branch too (very important)."

Translation, this round:

- Three new `@Test` methods (`userPromptAndDeterministicNoteSolvedBranchAnchorOnJustFinishedScenario`, `userPromptAndDeterministicNoteSolvedBranchHoldsWhenParallelScenarioIsRecentlySlipping`, `userPromptAndDeterministicNoteSolvedBranchNeverNamesParallelPriorCrossing`) appended to the existing `PostRepCoachNoteUserPromptAnchoringTests` suite, slotted immediately after `userPromptAndDeterministicNoteNameSameJustFinishedRepsScenario` so the TRAJECTORY-branch + SOLVED-branch cross-surface contracts read as one contiguous block. No new fixtures; the round-44 helpers + factory carry through verbatim.
- The redesign-branch invariant: round 45 is a `Redesign`-branch push per the user brief. It preserves the round-by-round loop on the redesign lineage that has been the home of rounds 11–44.

## What shipped

### Track 1 — Three SOLVED-branch cross-surface contract tests (`NoumTests/NoumTests.swift`)

Three new `@Test` methods on the existing `struct PostRepCoachNoteUserPromptAnchoringTests` suite (the round-44 suite), slotted under a new `// MARK: - Cross-surface contract: SOLVED-branch symmetric companion (round 45)` section immediately after the existing round-44 `userPromptAndDeterministicNoteNameSameJustFinishedRepsScenario` test and immediately before the closing brace of the suite. No new fixtures: each test uses the round-44 `imSession`, `slippingScenarioSessions`, `crossingScenarioSessions`, and `makeInput` helpers verbatim.

- **`userPromptAndDeterministicNoteSolvedBranchAnchorOnJustFinishedScenario`** — Baseline SOLVED cross-surface contract. Just-finished rep IS the crossing rep in scenario A (Difficult Conversation, calm); scenario B (Networking, confident) is one thin parallel rep. Asserts: prompt contains `"SOLVED this rep"`, `"Difficult Conversation"`, NOT `"Networking"`. Note (lowercased) contains `"difficult conversation"`, `"solved"`, NOT `"networking"`. Symmetric to round 44's `userPromptAndDeterministicNoteNameSameJustFinishedRepsScenario` on the TRAJECTORY surface.
- **`userPromptAndDeterministicNoteSolvedBranchHoldsWhenParallelScenarioIsRecentlySlipping`** — Defensive: same crossing-A fixture + 4-rep below-bar slip in scenario B (the loudest open trajectory in the session set). Both surfaces remain on scenario A's SOLVED win because the helper threads scenario A only through `toneDrillCrossing` and `toneDrillProgress`. Asserts: same `"SOLVED this rep"` + `"Difficult Conversation"` + NOT `"Networking"` shape on prompt; same `"difficult conversation"` + `"solved"` + NOT `"networking"` shape on note. The deterministic priority chain hits SOLVED first (branch 0 in `metricSentence(for:persona:)`), so the metric reads the SOLVED sentence even when B's trajectory would have been the loudest open thread.
- **`userPromptAndDeterministicNoteSolvedBranchNeverNamesParallelPriorCrossing`** — Defensive: same crossing-A fixture + scenario C (Work Update, professional) had a prior crossing rep at -20 days. The "you've already done it before" pivot is structurally impossible. Asserts: prompt contains `"SOLVED this rep"` + `"Difficult Conversation"`, NOT `"Work Update"` / `"Professional"` / lowercased `"work update"`. Note (lowercased) contains `"difficult conversation"` + `"solved"`, NOT `"work update"` / `"professional"`.

### Vision alignment

- **Coach-parity stage #4 (Adaptation) — "compare response across multiple attempts and either reinforce, vary, or replace the intervention with an explained rationale."** Per `docs/VISION.md`. Round 45 locks the SOLVED-branch Adaptation read on both the AI-prompt and deterministic-note surfaces in lockstep: the chat coach's Adaptation read (rounds 41/42 surface, freshness-gated), the post-rep coach note's deterministic-path Adaptation read (round 43 surface, just-finished-rep-scenario gate), the post-rep coach note's AI-path Adaptation read (round 44 surface, same just-finished-rep-scenario gate), AND now the SOLVED-branch cross-surface contract (round 45) all have testable structural anchoring contracts. The same SOLVED read on both surfaces — pinned in lockstep, behaviorally inseparable from the helper's resolved trio anchoring.
- **Coach-parity stage #3 (Intervention) — "prescribe a drill for a reason, name the observable target, and define what improvement would look like before the user starts."** The SOLVED branch is the terminal state of the Intervention loop: the moment the prescribed drill closes. Round 45 pins both surfaces to the just-finished rep's scenario on the SOLVED clause so neither path can headline a stale "you've done it before" win in a different scenario, even when one exists in the user's history. Both paths reflect the same Intervention-closure read.
- **Pillar #5 (Personalized coaching).** A coach (deterministic OR AI) who names the wrong scenario's win after a fresh crossing rep — pivoting from the rep the user just produced to an unrelated old win in another scenario — reads as a coach who isn't paying attention to the just-completed crossing rep. Round 45 makes the per-rep SOLVED anchoring structurally guaranteed on both paths in lockstep. The user can't tell which path produced their note (the surface labels it `isAIBacked: true/false`), so the SOLVED anchoring contract must hold on both — and now is pinned by tests that drive both surfaces simultaneously.
- **Pillar #4 (Believable progress).** The SOLVED clause is the most coaching-significant signal a post-rep note can carry — the moment a real coach earns trust. Round 45 makes the SOLVED-on-the-right-scenario contract testable on both surfaces in one method, so a future regression on either path is caught before it ships to the user.
- **Pillar #3 (Conversational intelligence).** The AI prompt's metric chain (filler, score, pace, etc.) is preserved verbatim. The IM tone-drill SOLVED clause (line ~1348 in `PostRepCoachNoteService.swift`) reads from the same `imToneDrillResolved*` input fields the round-43 helper anchors and the deterministic note's branch 0 (line ~619 in `PostRepCoachNoteService.swift`) reads — same wire, same surface, same brand voice. Round 45 just makes the two surfaces' SOLVED reads testably inseparable.
- **Anti-overclaim.** No engine state mutation, no new freshness gate, no new constants, no schema bump, no migration, no production code change at all. The refactor is a no-op on the behavior side; the tests pin the cross-surface structural contract that was already true.
- **Engineering bans.** No placeholder logic. No dead toggles. No fragmented state. The three new tests READ only the existing `PracticeSession.id` + `PracticeSession.mode` + `PracticeSession.imConversationDetails.setup.scenario` + `PracticeSession.imConversationDetails.setup.targetTone` fields plus the existing `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)` helper output (via the round-44 `makeInput` factory). No new storage, no schema bump, no migration, no new view inputs. The existing `PostRepCoachNoteService.userPrompt(from:)`, `PostRepCoachNoteService.deterministicNote(input:)`, `PostRepCoachNoteService.metricSentence(for:persona:)`, `PostRepCoachNoteService.imToneResolvedSentence(...)`, and `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)` paths are preserved verbatim. Pure test addition on pure-function surfaces.

### Branch + redesign-alignment notes

- All tracks land on `Redesign`, the redesign-lineage branch the rolling M24 deferred-slate work has been shipping on since round 11. The user brief explicitly calls this out: "ensure working on the redesign branch too (very important)." Round 45 preserves the round-by-round loop on the redesign lineage.
- Round 45 does not change any of the round-44 / round-43 / round-42 / round-41 / round-40 / ... / round-26 surfaces; every prior suite remains unchanged; the round-45 3 new `@Test` methods sit at the end of the existing `PostRepCoachNoteUserPromptAnchoringTests` suite (the round-44 suite). The round-44 `PostRepCoachNoteUserPromptAnchoringTests` (12 tests, now 15 with round 45's additions), round-43 `PostRepCoachNoteJustFinishedRepAnchoringTests` (14 tests), round-42 `ToneDrillTrajectoryFreshnessTests` (15 tests), round-41 `ToneDrillSolvedFreshnessTests` (11 tests), and every other suite are preserved verbatim.
- The existing `PostRepCoachNoteToneTrajectoryTests` (5 tests) and `PostRepCoachNoteToneResolvedTests` are preserved verbatim — round 45 is purely additive on the test side, no fixture or production touch.

## Future moves

(Updated priority list — round 45 closed the SOLVED-branch cross-surface contract, the deferred move #25 from round 44. The rest roll forward.)

1. **Peer Sudden Death scores via `FriendsManager`.** Still blocked on `PublicProfileSnapshot` schema work.
2. **`coachNoteRevealed` cleanup.** Still risky — animation chain interleaving with celebration timing. Worth a dedicated refactor pass with proper visual QA (and a real device).
3. **Visual polish pass on the round-19 launch CTA.** Carried forward from rounds 19–44. Pure visual work, not destination logic.
4. **Visual polish pass on the round-20 SOLVED ribbon.** Carried forward from rounds 20–44. Pure visual work, not crossing logic.
5. **Day-rollover refresh for long-mounted observers.** Carried forward from round 22.
6. **Tier-change observation symmetry to other surfaces that read `AIRateLimiter.currentCap()` directly.** Carried forward from round 23.
7. **Refresh-on-rotate for the empty-state chip when the `CoachMemoryStore` mutates while AskNoumView is mounted.** Carried forward from round 25.
8. **Voice-tuned ack-chip glyphs.** Carried forward from round 26.
9. **Engine-level case-anchored amplification (post-round-37 escalation).** Carried forward from round 37 as a future move. Restraint pin: hold until real-conversation evidence calls for engine-level escalation.
10. **Engine-level under-repeated-pushback dampening (post-round-38 escalation).** Carried forward from round 38. Restraint pin: hold for real-conversation evidence.
11. **Engine-level case-anchored continuation (post-round-39 escalation).** Carried forward from round 39. Restraint pin: hold for real-conversation evidence.
12. **Engine-level under-pushback continuation dampening (post-round-40 escalation).** Carried forward from round 40. Restraint pin: hold for real-conversation evidence.
13. **Collapse the round-26 hypothesis-ack reflection in `coachCaseFormulationLines` into a single block with the round-32 rebuild-verdict lines when the predicate fires.** Carried forward from round 32. Hold for real-device QA.
14. **Trend-view distinction across the rebuild-verdict / case-state matrix surfaces.** Carried forward from rounds 30–44. Pure visual work; hold for real-device QA.
15. **Second-cycle ask register on the opener.** Note from round 35. Restraint pin: hold until real conversations show the cycle-agnostic ask reads as off.
16. **`CaseReviewCard` adaptation-log fuller history surface.** Note from round 36. Hold for real-device QA.
17. **`CaseReviewCard` case-anchored badge.** Note from round 37. Hold for real-device QA.
18. **`CaseReviewCard` under-pushback badge.** Note from round 38. Hold for real-device QA.
19. **`CaseReviewCard` case-anchored continuation badge.** Note from round 39. Hold for real-device QA.
20. **`CaseReviewCard` under-pushback continuation badge.** Note from round 40. Hold for real-device QA.
21. **Apply the freshness window to the `IMToneDrillProgress.stalled` chat-coach read separately from `recovering`/`slipping`.** Carried forward from round 42. Restraint pin: hold for real-conversation evidence of the stalled-and-stuck dynamic before splitting the window.
22. **Lift the recent-summaries + recent-proofs construction into `MomentumComputer` (or a sibling pure helper) for parallel anchoring tests.** Carried forward from round 43. The finalizer's `recentSessionSummaries` and `recentProofQuotes` are constructed inline in `recordPostRepCoachNote` (the same shape as the pre-round-43 IM tone-drill block). They DON'T have the same cross-rep cold-open concern (they're explicitly cross-rep summaries by design), but lifting them into pure helpers would make the per-rep finalizer wire more uniformly testable. Pure refactor; no behavior change; hold for a slow round.
23. **Defensive pin on `PostRepCoachNoteStore.record(_:)` dedupe-by-sessionID contract.** Carried forward from round 43. The store de-duplicates notes by sessionID (the finalize → AI upgrade → re-record path relies on this); a defensive store-level test pinning the dedupe contract and the "never-write-for-deleted-session" guard would lock the surface symmetric to the round-43 helper. Hold for the right round.
24. **Unify the round-43 + round-44 fixture helpers (`imSession`, `nonIMSession`, `slippingScenarioSessions`, `crossingScenarioSessions`) into one private `IMHelpers` namespace shared across both suites.** Carried forward from round 44. The round-44 suite redeclares all four helpers verbatim from round 43 for self-containment; the round-45 tests reuse the round-44 helpers but a future refactor lifts them into a shared namespace so a future test author doesn't need to keep four helpers in sync across the trajectory + deterministic-anchoring + AI-anchoring + cross-surface + resolved suites. Pure test refactor; no behavior change; hold for a slow round.
25. **AI-path tests for the metric branches (filler, score, pace) routed through `userPrompt(from:)`.** Carried forward from round 44. The round-43 + round-44 + round-45 contract is locked on the IM tone-drill clauses; the per-rep metric branches (filler comparison vs baseline, zero-fillers, score bands, etc.) construct prompt lines from the same `PostRepCoachNoteInput` fields. A defensive suite mirroring round-44's shape (12 tests, fixture-driven, route through `userPrompt(from:)`) would extend the same anchoring contract surface to the metric branches. Hold for the right round.
26. **Cross-surface contract for the consecutive-clean-reps / personal-best / filler-trend momentum lines.** New note from round 45. The same `userPromptAndDeterministicNote*` pattern that locks the IM tone-drill clauses on both surfaces could extend to the non-IM momentum lines — consecutive clean reps (line ~1322 in `userPrompt`), personal best (~1325), filler trend (~1330), score trend (~1333), weekly rep count (~1336). The deterministic priority chain reads these in branches 0a–0d (line ~627-662 in `metricSentence`). A symmetric cross-surface suite (one test per momentum branch) would pin both paths in lockstep. Hold for the right round.
27. **End-to-end cross-surface contract that routes through `PracticeSessionFinalizer.recordPostRepCoachNote`.** New note from round 45. The current cross-surface contracts (round 44 TRAJECTORY + round 45 SOLVED) route through the pure `userPrompt(from:)` + `deterministicNote(input:)` surfaces directly, bypassing the finalizer wire. A defensive end-to-end test that constructs a `PracticeSession` set, invokes the finalizer, and asserts the persisted `PostRepCoachNote.noteText` headlines scenario A would lock the FULL wire (finalizer → helper → input → deterministic-or-AI note). Higher complexity (the finalizer takes a `CoachMemoryStore`, `PracticeSessionsStore`, etc.); hold for a build-host-equipped round.
28. **Cross-surface contract test extending the round-45 SOLVED pattern to the TRAJECTORY + thin-just-finished case.** New note from round 45. Round 44's `userPromptAndDeterministicNoteNameSameJustFinishedRepsScenario` covers recovering-A + slipping-B; round 45's 3 tests cover crossing-A on three angles. A symmetric companion would test: thin just-finished rep in A (no qualifying trajectory) + slipping-B (loud open trajectory) — both surfaces stay silent on B even though it's the only qualifying trajectory in the session set. The existing single-surface tests (`userPromptNeverNamesAnotherScenarioWhenJustFinishedRepIsThin` + `deterministicNoteNeverNamesAnotherScenarioWhenJustFinishedRepIsThin`) cover this per-surface; the cross-surface lockstep test is the deferred move. Hold for the right round.

## Build-host limitation (honest note for the next agent)

This environment has **no Xcode and no Swift toolchain**, so nothing in this round was compiled or run — not the app, not the test suite. The change is:

- Three new `@Test` methods (`userPromptAndDeterministicNoteSolvedBranchAnchorOnJustFinishedScenario`, `userPromptAndDeterministicNoteSolvedBranchHoldsWhenParallelScenarioIsRecentlySlipping`, `userPromptAndDeterministicNoteSolvedBranchNeverNamesParallelPriorCrossing`) appended to the existing `PostRepCoachNoteUserPromptAnchoringTests` suite in `NoumTests/NoumTests.swift`. Slotted immediately after the existing round-44 `userPromptAndDeterministicNoteNameSameJustFinishedRepsScenario` test and immediately before the suite's closing brace. No new fixtures.

All checks the next agent should run on a real build host:

1. `swift test --filter PostRepCoachNoteUserPromptAnchoringTests` — the suite now has 15 tests (round-44's 12 + round-45's 3); all should pass.
2. `swift test --filter PostRepCoachNoteJustFinishedRepAnchoringTests` — round-43's 14 tests should still pass verbatim (the round-45 additions are scoped to the round-44 suite).
3. `swift test --filter PostRepCoachNoteToneTrajectoryTests` — round-15's existing tests should still pass verbatim.
4. `swift test --filter PostRepCoachNoteToneResolvedTests` — the existing SOLVED-branch tests should still pass verbatim.
5. `swift test --filter ToneDrillTrajectoryFreshnessTests` — round-42's 15 tests should still pass verbatim.
6. `swift test --filter ToneDrillSolvedFreshnessTests` — round-41's 11 tests should still pass verbatim.
7. `swift test --filter ToneDrillTrajectoryContextTests` — round-12's 8 tests should still pass.
8. `swift test --filter ToneDrillResolvedContextTests` — round-13's 4 tests should still pass.
9. `swift test --filter IMToneDrillSignalTests` — round-12/14's primitive tests should still pass.
10. `swift test --filter InterventionUnderPushbackContinuationTests` — round-40's 29 tests should still pass.
11. `swift test --filter CaseAnchoredContinuationTests` — round-39's 28 tests should still pass.
12. `swift test --filter CaseAnchoredAmplificationTests` — round-37's 27 tests should still pass.
13. `swift test --filter InterventionUnderRepeatedPushbackTests` — round-38's 32 tests should still pass.
14. `swift test --filter RebuildVerdictContextTests` — round-32's 25 tests should still pass.
15. `swift test --filter FreshRevisedReadContextTests` — round-31's 16 tests should still pass.
16. Boot the app on simulator, finish an IM rep in scenario A (e.g. Difficult Conversation, calm) that IS the crossing rep — the just-finished rep is the one that pushes scenario A's committed tone across the drill bar — while the user ALSO has parallel signals in another scenario (e.g. a recent 4-rep below-bar slip in Networking, or an old crossing rep in Work Update from weeks ago). Confirm the post-rep Coach's Read card on the Summary screen headlines scenario A's SOLVED win ("Your calm tone in Difficult Conversation is solved — …") and NEVER mentions any other scenario's title. This is the visual confirmation of the cross-surface SOLVED contract round 45 locks at the helper + prompt + note layers.
17. With the AI provider configured (when available), repeat the test in step 16 and confirm the AI-backed SOLVED note also stays on scenario A's surface. The AI path's structural impossibility of a cross-rep SOLVED cold open is now both architecturally enforced AND tested at the prompt layer.
18. Confirm the round-21 SOLVED ribbon on the hero score card continues to fire exactly once on the crossing rep — same primitive (`toneDrillCrossing`) routes both surfaces through the helper, so the two surfaces remain in lockstep.
19. Confirm the round-26 hypothesis-ack chip, round-32 rebuild-verdict line, round-37/38/39/40 case-state matrix, round-31 fresh-revised-read context block, round-41 SOLVED freshness window, round-42 TRAJECTORY freshness window, round-43 deterministic-path anchoring, round-44 AI-path anchoring, and round-45 cross-surface SOLVED contract are all preserved verbatim.
