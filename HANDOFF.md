# HANDOFF — M24 deferred slate (round 47): stalled-trajectory cross-surface contract — both `PostRepCoachNoteService.userPrompt(from:)` AND `PostRepCoachNoteService.deterministicNote(input:)` stay silent on the IM tone-drill trajectory clause when the just-finished rep's scenario is in a stalled trajectory (delta in (-15%, +15%), a flat arc not worth coaching language), AND neither surface pivots to a loud parallel scenario's open thread (slipping or recovering) when A's read offers no coaching language. Round 47 closes future move #28 from round 46's HANDOFF ("stalled-trajectory cross-surface contract"). Pure test addition; no production code change, no engine state change, no schema bump, no migration, no new view inputs. The structural contract was already true at round 12/15 (the helper's stalled gate on `direction != .stalled` is fixed and the prompt + deterministic note independently honor it) and was pinned per-surface at round 44 (`userPromptOmitsTrajectoryClauseOnStalledDirection`) and round 15's `PostRepCoachNoteToneTrajectoryTests` (the deterministic-path stalled fall-through). Round 47 locks the two surfaces in lockstep on the stalled-just-finished branch with three new `@Test` methods on the existing `PostRepCoachNoteUserPromptAnchoringTests` suite so a future regression on EITHER surface (helper, prompt, deterministic note) trips the cross-surface contract independently of which surface broke first. The 2x2 direction × locus matrix is now structurally pinned in lockstep across rounds 44 (recovering-A loud) + 45 (crossing-A loud × 3 angles) + 46 (thin-A silent × 3 angles) + 47 (stalled-A silent × 3 angles).

## Scope

Rounds 44 + 45 + 46 pin the **loud + silent-from-emptiness** corners of the cross-surface contract: recovering-A and crossing-A on the loud side (round 44 single + round 45 three angles), thin-A on the silent-from-emptiness side (round 46 three angles). The fourth corner — **silent-from-flatness** — is the stalled trajectory: scenario A has 4+ evaluated reps so the helper returns a non-nil `IMToneDrillProgress`, but `direction == .stalled` (delta in (-15%, +15%)) so both surfaces gate the trajectory clause off independently. Future move #28 noted: "A defensive cross-surface test pinning the stalled gate in lockstep (both surfaces silent on a stalled-A trajectory, AND silent on parallel-B when A is stalled) would close the 2x2 direction × locus matrix. Hold for a slow round."

Round 47 closes that gap. Three new `@Test` methods on `PostRepCoachNoteUserPromptAnchoringTests`, slotted at the end of the suite immediately after the round-46 `userPromptAndDeterministicNoteThinJustFinishedNeverNameParallelPriorCrossing` test, lock the stalled-trajectory cross-surface contract on three angles:

1. **Baseline stalled-A own-scenario** (`userPromptAndDeterministicNoteStalledJustFinishedNeverProducesOwnTrajectoryClause`) — just-finished rep is the 4th rep in scenario A (Networking, confident) with an alternating off/on/off/on hit pattern (actualTones: shaky, confident, shaky, confident). The helper returns `IMToneDrillProgress(direction: .stalled, earlierRate: 0.5, recentRate: 0.5, windowSize: 2)`. Both surfaces' trajectory clauses gate on `direction != .stalled`, so neither emits the "tone-match" phrasing. `toneDrillResolved` ALSO returns nil because `progress.earlierRate (0.5) >= matchRateThreshold (0.4)` — the user never sat below the bar — so the SOLVED clause never fires either. Both IM branches are dark on the helper's output.
2. **Stalled-A + parallel-slipping-B** (`userPromptAndDeterministicNoteStalledJustFinishedNeverNameParallelSlippingScenario`) — same stalled-A just-finished + scenario B (Difficult Conversation, calm) carries a 4-rep below-bar slip. The "let me talk about scenario B's slip" pivot is structurally impossible: the helper threads scenario A only through `toneDrillProgress`, so B's slip is invisible to both surfaces. Both stay silent on B's title.
3. **Stalled-A + parallel-recovering-B** (`userPromptAndDeterministicNoteStalledJustFinishedNeverNameParallelRecoveringScenario`) — same stalled-A + scenario B carries a 4-rep recovering trajectory at an earlier window. The "you're recovering elsewhere" pivot is structurally impossible: same anchoring contract.

The three tests together pin both surfaces in lockstep on the stalled-just-finished branch the same way round 44's single test + round 45's three tests + round 46's three tests do for the recovering / crossing / thin branches: a future regression that lets EITHER surface (helper, prompt, or deterministic note) emit a tone-match clause when the just-finished rep's scenario is stalled, OR name a parallel scenario when A is stalled, trips at least one of the three round-47 tests independently of which surface broke first.

Defensive scoping (intentional restraint, same shape as rounds 41/42/43/44/45/46):

- **Pure test addition, no production code change.** Round 47 modifies only `NoumTests/NoumTests.swift` and `HANDOFF.md`. Every production surface (`MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)`, `PostRepCoachNoteService.userPrompt(from:)`, `PostRepCoachNoteService.deterministicNote(input:)`, `PostRepCoachNoteService.metricSentence(for:persona:)`, `PostRepCoachNoteService.imToneTrajectorySentence(...)`, `IMHistorySummary.toneDrillProgress(from:scenario:)`, `IMHistorySummary.toneDrillCrossing(...)`) is preserved verbatim. The structural contract round 47 pins was already true at round 12/15 (helper-level stalled gate) and pinned per-surface at round 44 (AI-path) and round 15 (deterministic-path); round 47 locks the two surfaces in lockstep on a fixture that lands the stalled trajectory through the helper, not a constructed-by-hand `IMToneDrillProgress`.
- **No new freshness gate.** Same rationale as rounds 43/44/45/46: the post-rep note is written EXACTLY ONCE, immediately after the rep finalizes, against signals computed strictly for the just-finished rep's scenario. The per-rep anchoring IS the freshness contract on both the deterministic and AI paths. Round 47 does NOT introduce a recency window.
- **No engine state change.** `IMHistorySummary.toneDrillProgress(from:scenario:)`, `IMHistorySummary.toneDrillCrossing(in:scenario:currentRepId:)`, `IMHistorySummary.toneDrillResolved(...)`, and `MomentumComputer.imToneDrillNoteFields` are unchanged. Both `PostRepCoachNoteService.userPrompt(from:)` and `PostRepCoachNoteService.deterministicNote(input:)` bodies are unchanged. No new persisted state, no schema bump, no migration, no new view inputs.
- **Both surfaces are `nonisolated static`.** Round 47 drives both the AI-path and deterministic-path surfaces directly without AI provider stubs — the same testability levers rounds 43 + 44 + 45 + 46 used. No mock provider, no async wiring, no rate-limit gymnastics.
- **Test fixtures mirror rounds 43–46's verbatim.** `imSession(id:scenario:targetTone:actualTone:daysOffset:)`, `slippingScenarioSessions(...)`, and the `makeInput(justFinished:allSessions:score:)` factory — same shapes the round-44/45/46 suite already declares; the round-47 tests are net new methods on the existing suite, reusing the existing fixtures without duplicating them. The stalled-A fixture is built inline (4-rep alternating shape, analogous to round-46's inline recovering-B fixture), since the round-44/45/46 suite never abstracted "stalled" into a helper — the inline 4-rep two-on-two-off alternation stays readable and self-contained. The parallel-recovering fixture in test 3 is also built inline (4-rep two-off-two-on shape, same as the round-43 `recoveringA` inline fixture and round-46's test 2 recovering-B fixture). The parallel-slipping fixture in test 2 reuses the existing `slippingScenarioSessions` helper verbatim. (Future move #24 — unifying the round-43 + round-44 helpers into a shared namespace — stays open; round 47 adds a 6th candidate to the namespace: a `stalledScenarioSessions(scenario:targetTone:offBrandTone:onBrandTone:earliestDayOffset:)` helper currently built inline in all three tests.)
- **Tests pin BOTH directions of structural impossibility on the stalled-just-finished branch.** Baseline own-scenario (stalled-A in isolation), parallel-slipping-B (the recommendation engine's next prescription), and parallel-recovering-B (the "you're recovering elsewhere" pivot) — three angles, all gated on the same `direction != .stalled` predicate on both surfaces, all pinned by end-to-end tests routed through the helper-then-prompt-and-note wire.
- **Each test asserts BOTH surfaces in one method.** A future regression that breaks anchoring on either surface trips the relevant test independently of which surface broke first — the cross-surface lockstep is the round-47 contribution. The existing round-44 `userPromptOmitsTrajectoryClauseOnStalledDirection` continues to fail-fast in isolation if the AI-path stalled gate regresses (and uses a hand-constructed progress that bypasses the helper, catching a surface-level regression). The round-47 tests add the cross-surface coupling and route through the helper, so a helper-level regression (e.g., the stalled threshold changes silently) also trips them.

User brief, unchanged round to round: "continue from the existing TO-DO, ensure working towards getting the app towards the vision plan, and all round A+, make my dream I had come true too, ensure working on the redesign branch too (very important)."

Translation, this round:

- Three new `@Test` methods (`userPromptAndDeterministicNoteStalledJustFinishedNeverProducesOwnTrajectoryClause`, `userPromptAndDeterministicNoteStalledJustFinishedNeverNameParallelSlippingScenario`, `userPromptAndDeterministicNoteStalledJustFinishedNeverNameParallelRecoveringScenario`) appended to the existing `PostRepCoachNoteUserPromptAnchoringTests` suite, slotted immediately after `userPromptAndDeterministicNoteThinJustFinishedNeverNameParallelPriorCrossing` so the recovering / crossing / thin / stalled cross-surface contracts read as one contiguous block. No new fixtures.
- The redesign-branch invariant: round 47 is a `Redesign`-branch push per the user brief. It preserves the round-by-round loop on the redesign lineage that has been the home of rounds 11–46.

## What shipped

### Track 1 — Three stalled-trajectory cross-surface contract tests (`NoumTests/NoumTests.swift`)

Three new `@Test` methods on the existing `struct PostRepCoachNoteUserPromptAnchoringTests` suite (the round-44/45/46 suite, now 21 tests), slotted under a new `// MARK: - Cross-surface contract: stalled just-finished trajectory (round 47)` section immediately after the existing round-46 `userPromptAndDeterministicNoteThinJustFinishedNeverNameParallelPriorCrossing` test and immediately before the closing brace of the suite. The stalled-A fixture in all three tests and the parallel-recovering-B fixture in test 3 are built inline (4-rep two-on-two-off alternation for stalled, 4-rep two-off-two-on for recovering — same shapes as the round-46 inline recovering-B fixture); the parallel-slipping-B fixture in test 2 reuses the existing `slippingScenarioSessions` helper verbatim.

- **`userPromptAndDeterministicNoteStalledJustFinishedNeverProducesOwnTrajectoryClause`** — Baseline stalled-just-finished own-scenario contract. Just-finished rep is the 4th rep in scenario A (Networking, confident, with alternating actualTones `["shaky", "confident", "shaky", "confident"]` across daysOffset -3, -2, -1, 0). The helper computes `progress = IMToneDrillProgress(direction: .stalled, earlierRate: 0.5, recentRate: 0.5, windowSize: 2)` (earlier prefix `[shaky, confident]` → 50% match; recent suffix `[shaky, confident]` → 50% match; delta = 0 → stalled). `resolved` is nil because `earlierRate (0.5) >= matchRateThreshold (0.4)` — the user was never below the bar. Asserts: prompt does NOT contain `"tone-match"`, `"SOLVED this rep"`, or `"Confident tone in Networking"`. Note (lowercased) does NOT contain `"confident tone in networking"`.
- **`userPromptAndDeterministicNoteStalledJustFinishedNeverNameParallelSlippingScenario`** — Defensive: same stalled-A just-finished + 4-rep below-bar slip in scenario B (Difficult Conversation, calm, all `tense`, daysOffset -7 to -4). The recommendation engine would prescribe B's drill next; both surfaces stay silent on B because the helper threads only scenario A through `toneDrillProgress`. Asserts: prompt does NOT contain `"tone-match"`, `"SOLVED this rep"`, `"Difficult Conversation"`, `"Calm tone in Difficult Conversation"`, or lowercased `"difficult conversation"`. Note (lowercased) does NOT contain `"difficult conversation"` or `"calm tone"`.
- **`userPromptAndDeterministicNoteStalledJustFinishedNeverNameParallelRecoveringScenario`** — Defensive: same stalled-A just-finished + 4-rep recovering trajectory in scenario B at an earlier window (daysOffset -10 to -7, actualTones `[tense, rushed, tense, calm]`). The "you're recovering elsewhere" pivot would be the loudest open positive thread; both surfaces stay silent on B because the helper threads only scenario A. Asserts: same shape as test 2.

### Vision alignment

- **Coach-parity stage #4 (Adaptation) — "compare response across multiple attempts and either reinforce, vary, or replace the intervention with an explained rationale."** Per `docs/VISION.md`. Round 47 closes the fourth and last corner of the 2x2 direction × locus matrix on the per-rep Adaptation read: rounds 44 + 45 pin the loud paths (recovering, crossing), round 46 pins the silent-from-emptiness path (thin), and round 47 pins the silent-from-flatness path (stalled). The chat coach's Adaptation read (rounds 41/42 surface, freshness-gated), the post-rep coach note's deterministic-path Adaptation read (round 43 surface), the post-rep coach note's AI-path Adaptation read (round 44 surface), the SOLVED-branch cross-surface contract (round 45), the TRAJECTORY + thin-just-finished cross-surface contract (round 46), AND now the stalled-trajectory cross-surface contract (round 47) all have testable structural anchoring contracts. The full 2x2 (recovering / crossing × loud / silent) and the additional (thin / stalled) corners are now structurally pinned in lockstep.
- **Coach-parity stage #3 (Intervention) — "prescribe a drill for a reason, name the observable target, and define what improvement would look like before the user starts."** Round 47 pins one of the most coaching-tempting failure modes: the just-finished rep's scenario has a flat trajectory (the helper has signal but it isn't directional), and a loud open thread sits elsewhere in the user's history. A real coach who pivots to "let me talk about your other scenario instead, since this one isn't moving" is no longer paying attention to the rep the user just produced — the right read on a stalled rep is to fall through to the per-rep metric chain (filler, score, pace), not to import a parallel scenario's drama. Round 47 makes that pivot structurally impossible on both surfaces in lockstep.
- **Pillar #5 (Personalized coaching).** A coach (deterministic OR AI) who, after a stalled rep in scenario A (the user's been on the same trajectory for weeks — no improvement, no regression), opens with "your calm tone in Difficult Conversation is slipping" or "you're recovering in another scenario" reads as a coach who hasn't read the flat trajectory in front of them. Round 47 makes the per-rep anchoring structurally guaranteed on both paths in lockstep even when the just-finished rep's read is the flat-line "no coaching language to add" case. The user can't tell which path produced their note (the surface labels it `isAIBacked: true/false`), so the stalled-anchoring contract must hold on both — and now is pinned by tests that drive both surfaces simultaneously.
- **Pillar #4 (Believable progress).** A stalled trajectory is the moment the post-rep note is most tempted to invent narrative drama. The temptation to pull from a noisier scenario in the session set ("the real story is over here") is exactly the failure mode a real coach avoids — the right answer is to read the per-rep metrics honestly and acknowledge the plateau, not to manufacture a different story. Round 47 makes that fall-back impossible on both surfaces in one method, so a future regression on either path is caught before it ships to the user.
- **Pillar #3 (Conversational intelligence).** The AI prompt's metric chain (filler, score, pace, etc.) is preserved verbatim. The IM tone-drill TRAJECTORY clause (line ~1339 in `PostRepCoachNoteService.swift`) and SOLVED clause (line ~1348) both gate on `direction != .stalled` and `imToneDrillResolved != nil` respectively; the deterministic note's branches 0 (SOLVED, line ~620) and 0d (TRAJECTORY, line ~655) gate identically. Round 47 just makes the two surfaces' stalled reads testably inseparable.
- **Anti-overclaim.** No engine state mutation, no new freshness gate, no new constants, no schema bump, no migration, no production code change at all. The change is a no-op on the behavior side; the tests pin the cross-surface structural contract that was already true.
- **Engineering bans.** No placeholder logic. No dead toggles. No fragmented state. The three new tests READ only the existing `PracticeSession.id` + `PracticeSession.mode` + `PracticeSession.imConversationDetails.setup.scenario` + `PracticeSession.imConversationDetails.setup.targetTone` + `PracticeSession.imConversationDetails.actualTone` fields plus the existing `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)` helper output (via the round-44/45/46 `makeInput` factory). No new storage, no schema bump, no migration, no new view inputs. The existing `PostRepCoachNoteService.userPrompt(from:)`, `PostRepCoachNoteService.deterministicNote(input:)`, `PostRepCoachNoteService.metricSentence(for:persona:)`, `IMHistorySummary.toneDrillProgress(from:scenario:)`, `IMHistorySummary.toneDrillCrossing(...)`, `IMHistorySummary.toneDrillResolved(...)`, and `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)` paths are preserved verbatim. Pure test addition on pure-function surfaces.

### Branch + redesign-alignment notes

- All tracks land on `Redesign`, the redesign-lineage branch the rolling M24 deferred-slate work has been shipping on since round 11. The user brief explicitly calls this out: "ensure working on the redesign branch too (very important)." Round 47 preserves the round-by-round loop on the redesign lineage.
- Round 47 does not change any of the round-46 / round-45 / round-44 / round-43 / ... / round-26 surfaces; every prior suite remains unchanged; the round-47 3 new `@Test` methods sit at the end of the existing `PostRepCoachNoteUserPromptAnchoringTests` suite. The round-46-extended `PostRepCoachNoteUserPromptAnchoringTests` (18 tests, now 21 with round 47's additions), round-43 `PostRepCoachNoteJustFinishedRepAnchoringTests` (14 tests), round-42 `ToneDrillTrajectoryFreshnessTests` (15 tests), round-41 `ToneDrillSolvedFreshnessTests` (11 tests), and every other suite are preserved verbatim.
- The existing `PostRepCoachNoteToneTrajectoryTests` (5 tests, including the deterministic-path stalled fall-through which round 47 sits alongside) and `PostRepCoachNoteToneResolvedTests` are preserved verbatim — round 47 is purely additive on the test side, no fixture or production touch.

## Future moves

(Updated priority list — round 47 closed the stalled-trajectory cross-surface contract, the deferred move #28 from round 46. The rest roll forward, plus new moves from round 47.)

1. **Peer Sudden Death scores via `FriendsManager`.** Still blocked on `PublicProfileSnapshot` schema work.
2. **`coachNoteRevealed` cleanup.** Still risky — animation chain interleaving with celebration timing. Worth a dedicated refactor pass with proper visual QA (and a real device).
3. **Visual polish pass on the round-19 launch CTA.** Carried forward from rounds 19–46. Pure visual work, not destination logic.
4. **Visual polish pass on the round-20 SOLVED ribbon.** Carried forward from rounds 20–46. Pure visual work, not crossing logic.
5. **Day-rollover refresh for long-mounted observers.** Carried forward from round 22.
6. **Tier-change observation symmetry to other surfaces that read `AIRateLimiter.currentCap()` directly.** Carried forward from round 23.
7. **Refresh-on-rotate for the empty-state chip when the `CoachMemoryStore` mutates while AskNoumView is mounted.** Carried forward from round 25.
8. **Voice-tuned ack-chip glyphs.** Carried forward from round 26.
9. **Engine-level case-anchored amplification (post-round-37 escalation).** Carried forward from round 37 as a future move. Restraint pin: hold until real-conversation evidence calls for engine-level escalation.
10. **Engine-level under-repeated-pushback dampening (post-round-38 escalation).** Carried forward from round 38. Restraint pin: hold for real-conversation evidence.
11. **Engine-level case-anchored continuation (post-round-39 escalation).** Carried forward from round 39. Restraint pin: hold for real-conversation evidence.
12. **Engine-level under-pushback continuation dampening (post-round-40 escalation).** Carried forward from round 40. Restraint pin: hold for real-conversation evidence.
13. **Collapse the round-26 hypothesis-ack reflection in `coachCaseFormulationLines` into a single block with the round-32 rebuild-verdict lines when the predicate fires.** Carried forward from round 32. Hold for real-device QA.
14. **Trend-view distinction across the rebuild-verdict / case-state matrix surfaces.** Carried forward from rounds 30–46. Pure visual work; hold for real-device QA.
15. **Second-cycle ask register on the opener.** Note from round 35. Restraint pin: hold until real conversations show the cycle-agnostic ask reads as off.
16. **`CaseReviewCard` adaptation-log fuller history surface.** Note from round 36. Hold for real-device QA.
17. **`CaseReviewCard` case-anchored badge.** Note from round 37. Hold for real-device QA.
18. **`CaseReviewCard` under-pushback badge.** Note from round 38. Hold for real-device QA.
19. **`CaseReviewCard` case-anchored continuation badge.** Note from round 39. Hold for real-device QA.
20. **`CaseReviewCard` under-pushback continuation badge.** Note from round 40. Hold for real-device QA.
21. **Apply the freshness window to the `IMToneDrillProgress.stalled` chat-coach read separately from `recovering`/`slipping`.** Carried forward from round 42. Restraint pin: hold for real-conversation evidence of the stalled-and-stuck dynamic before splitting the window. (Note: this is the chat-coach freshness gate, distinct from the round-47 stalled-anchoring contract on the post-rep note surfaces. Both surfaces honor stalled by gating it out; the chat-coach surface uses a freshness gate, the post-rep note surfaces use the per-rep helper anchoring.)
22. **Lift the recent-summaries + recent-proofs construction into `MomentumComputer` (or a sibling pure helper) for parallel anchoring tests.** Carried forward from round 43. The finalizer's `recentSessionSummaries` and `recentProofQuotes` are constructed inline in `recordPostRepCoachNote` (the same shape as the pre-round-43 IM tone-drill block). They DON'T have the same cross-rep cold-open concern (they're explicitly cross-rep summaries by design), but lifting them into pure helpers would make the per-rep finalizer wire more uniformly testable. Pure refactor; no behavior change; hold for a slow round.
23. **Defensive pin on `PostRepCoachNoteStore.record(_:)` dedupe-by-sessionID contract.** Carried forward from round 43. The store de-duplicates notes by sessionID (the finalize → AI upgrade → re-record path relies on this); a defensive store-level test pinning the dedupe contract and the "never-write-for-deleted-session" guard would lock the surface symmetric to the round-43 helper. Hold for the right round.
24. **Unify the round-43 + round-44 fixture helpers (`imSession`, `nonIMSession`, `slippingScenarioSessions`, `crossingScenarioSessions`) into one private `IMHelpers` namespace shared across both suites.** Carried forward from round 44. The round-44/45/46/47 suites reuse the round-43 helpers via redeclaration; the round-46 + round-47 tests build the recovering / stalled fixtures inline. A future refactor lifts them into a shared namespace so a future test author doesn't need to keep five+ helpers in sync. Round 47 adds a 6th candidate to the namespace: a `stalledScenarioSessions(scenario:targetTone:offBrandTone:onBrandTone:earliestDayOffset:)` helper currently built inline in all three round-47 tests. Pure test refactor; no behavior change; hold for a slow round.
25. **AI-path tests for the metric branches (filler, score, pace) routed through `userPrompt(from:)`.** Carried forward from round 44. The round-43 + round-44 + round-45 + round-46 + round-47 contract is locked on the IM tone-drill clauses; the per-rep metric branches (filler comparison vs baseline, zero-fillers, score bands, etc.) construct prompt lines from the same `PostRepCoachNoteInput` fields. A defensive suite mirroring round-44's shape (12 tests, fixture-driven, route through `userPrompt(from:)`) would extend the same anchoring contract surface to the metric branches. Hold for the right round.
26. **Cross-surface contract for the consecutive-clean-reps / personal-best / filler-trend momentum lines.** Note from round 45. The same `userPromptAndDeterministicNote*` pattern that locks the IM tone-drill clauses on both surfaces could extend to the non-IM momentum lines — consecutive clean reps (line ~1322 in `userPrompt`), personal best (~1325), filler trend (~1330), score trend (~1333), weekly rep count (~1336). The deterministic priority chain reads these in branches 0a–0d (line ~627-662 in `metricSentence`). A symmetric cross-surface suite (one test per momentum branch) would pin both paths in lockstep. Hold for the right round.
27. **End-to-end cross-surface contract that routes through `PracticeSessionFinalizer.recordPostRepCoachNote`.** Note from round 45. The current cross-surface contracts (round 44 TRAJECTORY + round 45 SOLVED + round 46 thin-just-finished + round 47 stalled) route through the pure `userPrompt(from:)` + `deterministicNote(input:)` surfaces directly, bypassing the finalizer wire. A defensive end-to-end test that constructs a `PracticeSession` set, invokes the finalizer, and asserts the persisted `PostRepCoachNote.noteText` headlines the just-finished rep's scenario (or stays silent when thin / stalled) would lock the FULL wire (finalizer → helper → input → deterministic-or-AI note). Higher complexity (the finalizer takes a `CoachMemoryStore`, `PracticeSessionsStore`, etc.); hold for a build-host-equipped round.
28. **Mixed-just-finished thin + own-scenario crossing-elsewhere-in-history cross-surface contract.** Note from round 46. The trickiest cold-open is: just-finished rep is thin (one rep in A), but the SAME scenario A had a CROSSING rep weeks ago that's now stale. The helper's `toneDrillCrossing(in:scenario:currentRepId:)` is anchored on `currentRepId`, so an old crossing in the same scenario but on a different rep is invisible to the just-finished rep's read — by construction. A defensive cross-surface test would pin this on both surfaces. Hold for the right round.
29. **Helper-output assertion layered into the cross-surface tests.** Note from round 46. The round-46 + round-47 baseline tests assert the *prompt + note text* don't name the parallel scenario, which is the user-facing contract. A defensive belt would also assert at the helper output: `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:).progress == nil && .resolved == nil` for the thin-A justFinished (round 46), AND `.progress?.direction == .stalled && .resolved == nil` for the stalled-A justFinished (round 47). The single-surface round-43 test `helperReturnsNilProgressOnThinJustFinishedRep` covers the thin case on the deterministic suite; lifting the assertion into the cross-surface suite would catch a helper regression independent of the surface tests. Hold for the right round.
30. **Stalled-just-finished + parallel-prior-crossing cross-surface contract.** New note from round 47. Round 47 covers stalled-A × (own / slipping-B / recovering-B); the fourth angle — stalled-A + prior-crossing-C — symmetric to round 46's third test (thin-A + prior-crossing-C). The "you've done it before in another scenario, here's a flat trajectory in front of you — pivot to C's old win" cold-open is structurally impossible: the helper threads only A through `toneDrillCrossing`, so C's old win is invisible to both surfaces. Defensive symmetric companion; hold for the right round.
31. **Stalled-A own-scenario at the 0%-stalled and 100%-stalled extremes.** New note from round 47. The round-47 baseline uses 50%-stalled (alternating off/on). The two extremes — 0%-stalled (all off-brand: helper returns stalled at earlierRate=0, recentRate=0; resolved is nil because the user never climbed above the bar) and 100%-stalled (all on-brand: helper returns stalled at earlierRate=1, recentRate=1; resolved is nil because the user never sat below the bar) — should also be pinned to lock the gate on the boundary cases. Defensive companion; hold for the right round.
32. **Helper-level test that `toneDrillProgress` returns `direction == .stalled` for the round-47 alternating fixture.** New note from round 47. The round-47 cross-surface tests assert the surface outputs don't contain "tone-match"; a helper-level test asserting `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:).progress?.direction == .stalled` for the same fixture would catch a helper regression that shifted the stalled boundary silently (e.g., the threshold drifts to 0.05 and the fixture starts reading as recovering or slipping). Defensive belt; hold for the right round.

## Build-host limitation (honest note for the next agent)

This environment has **no Xcode and no Swift toolchain**, so nothing in this round was compiled or run — not the app, not the test suite. The change is:

- Three new `@Test` methods (`userPromptAndDeterministicNoteStalledJustFinishedNeverProducesOwnTrajectoryClause`, `userPromptAndDeterministicNoteStalledJustFinishedNeverNameParallelSlippingScenario`, `userPromptAndDeterministicNoteStalledJustFinishedNeverNameParallelRecoveringScenario`) appended to the existing `PostRepCoachNoteUserPromptAnchoringTests` suite in `NoumTests/NoumTests.swift`. Slotted immediately after the existing round-46 `userPromptAndDeterministicNoteThinJustFinishedNeverNameParallelPriorCrossing` test and immediately before the suite's closing brace. The stalled-A fixture is built inline (4-rep alternating off/on/off/on shape), the parallel-recovering-B fixture in test 3 is built inline (4-rep two-off-two-on shape), and the parallel-slipping-B fixture in test 2 reuses the existing `slippingScenarioSessions` helper verbatim.

All checks the next agent should run on a real build host:

1. `swift test --filter PostRepCoachNoteUserPromptAnchoringTests` — the suite now has 21 tests (round-44's 12 + round-45's 3 + round-46's 3 + round-47's 3); all should pass.
2. `swift test --filter PostRepCoachNoteJustFinishedRepAnchoringTests` — round-43's 14 tests should still pass verbatim (the round-47 additions are scoped to the round-44/45/46 suite).
3. `swift test --filter PostRepCoachNoteToneTrajectoryTests` — round-15's existing tests (including the deterministic-path stalled fall-through) should still pass verbatim.
4. `swift test --filter PostRepCoachNoteToneResolvedTests` — the existing SOLVED-branch tests should still pass verbatim.
5. `swift test --filter ToneDrillTrajectoryFreshnessTests` — round-42's 15 tests should still pass verbatim.
6. `swift test --filter ToneDrillSolvedFreshnessTests` — round-41's 11 tests should still pass verbatim.
7. `swift test --filter ToneDrillTrajectoryContextTests` — round-12's 8 tests should still pass.
8. `swift test --filter ToneDrillResolvedContextTests` — round-13's 4 tests should still pass.
9. `swift test --filter IMToneDrillSignalTests` — round-12/14's primitive tests should still pass.
10. `swift test --filter InterventionUnderPushbackContinuationTests` — round-40's 29 tests should still pass.
11. `swift test --filter CaseAnchoredContinuationTests` — round-39's 28 tests should still pass.
12. `swift test --filter CaseAnchoredAmplificationTests` — round-37's 27 tests should still pass.
13. `swift test --filter InterventionUnderRepeatedPushbackTests` — round-38's 32 tests should still pass.
14. `swift test --filter RebuildVerdictContextTests` — round-32's 25 tests should still pass.
15. `swift test --filter FreshRevisedReadContextTests` — round-31's 16 tests should still pass.
16. Boot the app on simulator, finish an IM rep in scenario A (e.g. Networking, confident) where the tone-match rate has been hovering near 50% for weeks (the "stalled" plateau dynamic) while the user ALSO has parallel signals in another scenario (e.g. a recent 4-rep below-bar slip in Difficult Conversation, or a 4-rep recovering trajectory in Difficult Conversation from weeks ago). Confirm the post-rep Coach's Read card on the Summary screen speaks to scenario A's per-rep metrics (filler, score, pace, weekly rep count) — NEVER emits a "tone-match" clause referring to A's flat trajectory, and NEVER mentions the parallel scenario's title in the IM tone-drill block. The card should fall through to a metric-chain read, not a tone-drill clause referring to A or B.
17. With the AI provider configured (when available), repeat the test in step 16 and confirm the AI-backed note also stays on scenario A's metrics and never emits a "tone-match" clause or names the parallel scenario in the TRAJECTORY clause. The AI path's structural impossibility of a stalled-A trajectory clause OR a cross-rep cold open is now both architecturally enforced AND tested at the prompt layer.
18. Confirm the round-21 SOLVED ribbon on the hero score card does NOT fire on a stalled just-finished rep in A — same primitive (`toneDrillCrossing` → `toneDrillResolved`) routes both surfaces through the helper, so the two surfaces remain in lockstep with the ribbon.
19. Confirm the round-26 hypothesis-ack chip, round-32 rebuild-verdict line, round-37/38/39/40 case-state matrix, round-31 fresh-revised-read context block, round-41 SOLVED freshness window, round-42 TRAJECTORY freshness window, round-43 deterministic-path anchoring, round-44 AI-path anchoring, round-45 cross-surface SOLVED contract, round-46 cross-surface thin-just-finished contract, and round-47 cross-surface stalled-just-finished contract are all preserved verbatim.
