# HANDOFF — M24 deferred slate (round 48): stalled just-finished boundary completion — both `PostRepCoachNoteService.userPrompt(from:)` AND `PostRepCoachNoteService.deterministicNote(input:)` stay silent on the IM tone-drill clauses across the three remaining stalled angles: (1) stalled-A + parallel-prior-crossing-C (the "you've done it before — pivot to C's old win" cold-open is structurally impossible), (2) 0%-stalled own-scenario (the loudest off-brand-only signal still gates out — `direction == .stalled` closes the trajectory clause and `recentRate (0) < holdRate (0.6)` closes the SOLVED clause), and (3) 100%-stalled own-scenario (steady-state on-brand competence still gates out — `direction == .stalled` closes the trajectory clause and `earlierRate (1) >= matchRateThreshold (0.4)` closes the SOLVED clause). Round 48 closes future moves #30 + #31 from round 47's HANDOFF and completes the stalled-corner of the 2x2 direction × locus matrix the round-44 / round-45 / round-46 / round-47 cross-surface tests set up. Pure test addition; no production code change, no engine state change, no schema bump, no migration, no new view inputs. Three new `@Test` methods on the existing `PostRepCoachNoteUserPromptAnchoringTests` suite (the round-44/45/46/47 suite, now 24 tests), slotted immediately after round 47's `userPromptAndDeterministicNoteStalledJustFinishedNeverNameParallelRecoveringScenario` test. The full stalled corner — own-scenario (50% baseline + 0% boundary + 100% boundary) × parallel (slipping + recovering + prior-crossing) — is now structurally pinned in lockstep on both surfaces.

## Scope

Round 47 covered the 50%-alternating baseline on the own-scenario branch and the parallel-slipping-B / parallel-recovering-B defensives on the parallel branch. The two remaining gaps it explicitly called out:

- **Future move #30** (round 47 HANDOFF): "Stalled-just-finished + parallel-prior-crossing cross-surface contract. Round 47 covers stalled-A × (own / slipping-B / recovering-B); the fourth angle — stalled-A + prior-crossing-C — symmetric to round 46's third test (thin-A + prior-crossing-C). The 'you've done it before in another scenario, here's a flat trajectory in front of you — pivot to C's old win' cold-open is structurally impossible: the helper threads only A through `toneDrillCrossing`, so C's old win is invisible to both surfaces. Defensive symmetric companion; hold for the right round."
- **Future move #31** (round 47 HANDOFF): "Stalled-A own-scenario at the 0%-stalled and 100%-stalled extremes. The round-47 baseline uses 50%-stalled (alternating off/on). The two extremes — 0%-stalled (all off-brand: helper returns stalled at earlierRate=0, recentRate=0; resolved is nil because the user never climbed above the bar) and 100%-stalled (all on-brand: helper returns stalled at earlierRate=1, recentRate=1; resolved is nil because the user never sat below the bar) — should also be pinned to lock the gate on the boundary cases. Defensive companion; hold for the right round."

Round 48 closes both. Three new `@Test` methods on `PostRepCoachNoteUserPromptAnchoringTests`, slotted at the end of the suite immediately after the round-47 `userPromptAndDeterministicNoteStalledJustFinishedNeverNameParallelRecoveringScenario` test, lock the stalled-trajectory contract on the three remaining angles:

1. **Stalled-A + parallel-prior-crossing-C** (`userPromptAndDeterministicNoteStalledJustFinishedNeverNameParallelPriorCrossing`) — same 50%-alternating stalled-A just-finished from round 47 + scenario C (Work Update, professional) carries a prior crossing rep at -20 days (4-rep two-off-two-on shape via the existing `crossingScenarioSessions` helper). The "you've done it before in another scenario, here's a flat trajectory in front of you — pivot to C's old win" cold-open is the trickiest stalled-A failure mode: the helper has a non-nil progress for A (the stalled gate closes the TRAJECTORY clause), and the engine has a real crossing in history (the SOLVED clause could be tempted). Neither fires on either surface: the helper threads ONLY scenario A through `toneDrillCrossing`, so C's old win is invisible; A's own `toneDrillResolved` is nil because `earlierRate (0.5)` is NOT below `matchRateThreshold (0.4)`. Both IM branches dark on both surfaces. Direct symmetric companion to round 46's `userPromptAndDeterministicNoteThinJustFinishedNeverNameParallelPriorCrossing`.
2. **0%-stalled own-scenario** (`userPromptAndDeterministicNoteFullyOffBrandStalledJustFinishedNeverProducesOwnTrajectoryClause`) — 4 reps in scenario A (Networking, confident) all off-brand ("shaky"). Helper computes `progress = IMToneDrillProgress(direction: .stalled, earlierRate: 0.0, recentRate: 0.0, windowSize: 2)`. The recommendation engine still has a real drill to prescribe (the scenario is squarely below the bar — `toneDrillSignal` is active), but the per-rep post-rep note's IM tone-drill TRAJECTORY clause stays silent because `direction == .stalled` — the right per-rep read is the metric chain, not a coaching-shaped trajectory clause on a flat-line miss. The SOLVED clause stays silent too: `toneDrillResolved` requires `recentRate >= toneDrillResolvedHoldRate (0.6)`, and 0% is far below that — the user hasn't climbed at all. Both IM branches dark on both surfaces. This is the rate-extreme companion to round 47's 50%-alternating baseline.
3. **100%-stalled own-scenario** (`userPromptAndDeterministicNoteFullyOnBrandStalledJustFinishedNeverProducesOwnTrajectoryClause`) — 4 reps in scenario A all on-brand ("confident"). Helper computes `progress = IMToneDrillProgress(direction: .stalled, earlierRate: 1.0, recentRate: 1.0, windowSize: 2)`. The user is holding — the trajectory is flat at the top, not the bottom. The per-rep post-rep note's IM tone-drill TRAJECTORY clause stays silent because `direction == .stalled` (a held-at-100% trajectory is not coaching language — "you're still on-brand" reads as filler, not signal). The SOLVED clause stays silent too: the user never sat below the bar (`earlierRate (1.0)` is NOT below `matchRateThreshold (0.4)`), so the turnaround predicate gates off — there was no gap to close, so no "you solved this" coaching language fits. Both IM branches dark on both surfaces. The rate-extreme companion to round 47's 50%-alternating baseline on the opposite end of the gradient from the 0%-stalled test above.

The three tests together pin both surfaces in lockstep on the remaining stalled-just-finished angles the same way round 44's single test + round 45's three tests + round 46's three tests + round 47's three tests do for the recovering / crossing / thin / stalled-50%-and-parallel branches: a future regression that lets EITHER surface (helper, prompt, or deterministic note) emit a tone-match clause when the just-finished rep's scenario is stalled at ANY rate (0%, 50%, or 100%), OR name a parallel scenario with a prior crossing when A is stalled, trips at least one of the three round-48 tests independently of which surface broke first.

Defensive scoping (intentional restraint, same shape as rounds 41/42/43/44/45/46/47):

- **Pure test addition, no production code change.** Round 48 modifies only `NoumTests/NoumTests.swift` and `HANDOFF.md`. Every production surface (`MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)`, `PostRepCoachNoteService.userPrompt(from:)`, `PostRepCoachNoteService.deterministicNote(input:)`, `PostRepCoachNoteService.metricSentence(for:persona:)`, `PostRepCoachNoteService.imToneTrajectorySentence(...)`, `IMHistorySummary.toneDrillProgress(from:scenario:)`, `IMHistorySummary.toneDrillCrossing(...)`, `IMHistorySummary.toneDrillResolved(...)`) is preserved verbatim. The structural contract round 48 pins was already true at round 12/15 (helper-level stalled gate) and pinned per-surface at round 44 (AI-path) and round 15 (deterministic-path); round 48 locks the two surfaces in lockstep on three more fixtures that land stalled trajectories through the helper (not constructed-by-hand `IMToneDrillProgress`s), closing the boundary completion that round 47's HANDOFF deferred.
- **No new freshness gate.** Same rationale as rounds 43/44/45/46/47: the post-rep note is written EXACTLY ONCE, immediately after the rep finalizes, against signals computed strictly for the just-finished rep's scenario. The per-rep anchoring IS the freshness contract on both the deterministic and AI paths. Round 48 does NOT introduce a recency window.
- **No engine state change.** All `IMHistorySummary` helpers, `MomentumComputer.imToneDrillNoteFields`, both surface bodies — unchanged. No new persisted state, no schema bump, no migration, no new view inputs.
- **Both surfaces are `nonisolated static`.** Round 48 drives both the AI-path and deterministic-path surfaces directly without AI provider stubs — the same testability levers rounds 43–47 used. No mock provider, no async wiring, no rate-limit gymnastics.
- **Test fixtures mirror rounds 43–47's verbatim.** `imSession(id:scenario:targetTone:actualTone:daysOffset:)`, `crossingScenarioSessions(...)`, and the `makeInput(justFinished:allSessions:score:)` factory — same shapes the round-47 suite already declares. The stalled-A 50%-alternating fixture in the parallel-prior-crossing test is built inline (4-rep alternating shape, same as round 47's three tests). The 0%-stalled and 100%-stalled fixtures are built inline (4-rep all-off and all-on shapes, analogous to round 47's inline alternating fixture). The parallel prior-crossing-C fixture reuses the existing `crossingScenarioSessions` helper verbatim (same usage as round 45's three SOLVED tests and round 46's third test).
- **Tests pin BOTH directions of structural impossibility on the stalled-just-finished branch at all three rate points.** Stalled-A + prior-crossing-C closes the "old win in another scenario" pivot; 0%-stalled own closes the "all-miss extreme" boundary; 100%-stalled own closes the "all-hold extreme" boundary. Three angles, all gated on the same `direction != .stalled` predicate on both surfaces, all pinned by end-to-end tests routed through the helper-then-prompt-and-note wire.
- **Each test asserts BOTH surfaces in one method.** A future regression that breaks anchoring on either surface trips the relevant test independently of which surface broke first — the cross-surface lockstep is the round-48 contribution.

User brief, unchanged round to round: "continue from the existing TO-DO, ensure working towards getting the app towards the vision plan, and all round A+, make my dream I had come true too, ensure working on the redesign branch too (very important)."

Translation, this round:

- Three new `@Test` methods (`userPromptAndDeterministicNoteStalledJustFinishedNeverNameParallelPriorCrossing`, `userPromptAndDeterministicNoteFullyOffBrandStalledJustFinishedNeverProducesOwnTrajectoryClause`, `userPromptAndDeterministicNoteFullyOnBrandStalledJustFinishedNeverProducesOwnTrajectoryClause`) appended to the existing `PostRepCoachNoteUserPromptAnchoringTests` suite, slotted immediately after round 47's `userPromptAndDeterministicNoteStalledJustFinishedNeverNameParallelRecoveringScenario` so the recovering / crossing / thin / stalled cross-surface contracts read as one contiguous block, completing the stalled corner. No new fixtures.
- The redesign-branch invariant: round 48 is a `Redesign`-branch push per the user brief. It preserves the round-by-round loop on the redesign lineage that has been the home of rounds 11–47.

## What shipped

### Track 1 — Three stalled-trajectory boundary-completion cross-surface contract tests (`NoumTests/NoumTests.swift`)

Three new `@Test` methods on the existing `struct PostRepCoachNoteUserPromptAnchoringTests` suite (the round-44/45/46/47 suite, now 24 tests), slotted under a new `// MARK: - Cross-surface contract: stalled just-finished boundary completion (round 48)` section immediately after the existing round-47 `userPromptAndDeterministicNoteStalledJustFinishedNeverNameParallelRecoveringScenario` test and immediately before the closing brace of the suite. The stalled-A fixtures (50% alternating in test 1, 100% all-off in test 2, 100% all-on in test 3) and the parallel prior-crossing-C reuse follow the round-47 pattern verbatim.

- **`userPromptAndDeterministicNoteStalledJustFinishedNeverNameParallelPriorCrossing`** — Stalled-A + parallel-prior-crossing-C. Just-finished rep is the 4th rep in scenario A (Networking, confident) with alternating `["shaky", "confident", "shaky", "confident"]` (50%-stalled, same shape as round 47's baseline). Scenario C (Work Update, professional) carries a prior crossing rep at -20 days via the existing `crossingScenarioSessions` helper (offBrandTone "vague", onBrandTone "professional"). Asserts: prompt does NOT contain `"tone-match"`, `"SOLVED this rep"`, `"Work Update"`, `"Professional"`, or lowercased `"work update"`. Note (lowercased) does NOT contain `"work update"` or `"professional"`.
- **`userPromptAndDeterministicNoteFullyOffBrandStalledJustFinishedNeverProducesOwnTrajectoryClause`** — 0%-stalled own-scenario. Just-finished rep is the 4th rep in scenario A with `["shaky", "shaky", "shaky", "shaky"]` (all off-brand). Helper computes `progress = IMToneDrillProgress(direction: .stalled, earlierRate: 0.0, recentRate: 0.0, windowSize: 2)`. `resolved` is nil because `recentRate (0.0)` is NOT `>= toneDrillResolvedHoldRate (0.6)`. Asserts: prompt does NOT contain `"tone-match"`, `"SOLVED this rep"`, or `"Confident tone in Networking"`. Note (lowercased) does NOT contain `"confident tone in networking"`.
- **`userPromptAndDeterministicNoteFullyOnBrandStalledJustFinishedNeverProducesOwnTrajectoryClause`** — 100%-stalled own-scenario. Just-finished rep is the 4th rep in scenario A with `["confident", "confident", "confident", "confident"]` (all on-brand). Helper computes `progress = IMToneDrillProgress(direction: .stalled, earlierRate: 1.0, recentRate: 1.0, windowSize: 2)`. `resolved` is nil because `earlierRate (1.0)` is NOT `< matchRateThreshold (0.4)`. Asserts: same shape as test 2.

### Vision alignment

- **Coach-parity stage #4 (Adaptation) — "compare response across multiple attempts and either reinforce, vary, or replace the intervention with an explained rationale."** Per `docs/VISION.md`. Round 48 closes the boundary completion on the stalled corner of the 2x2 direction × locus matrix: rounds 44 + 45 pin the loud paths (recovering, crossing), round 46 pins the silent-from-emptiness path (thin × 3 angles), round 47 pins the silent-from-flatness baseline + parallel paths (stalled-50% × own / slipping-B / recovering-B), and round 48 pins the boundary extremes + the last cross-scenario angle (stalled-50% + prior-crossing-C, stalled-0% own, stalled-100% own). The chat coach's Adaptation read (rounds 41/42 surface, freshness-gated), the post-rep coach note's deterministic-path Adaptation read (round 43 surface), the post-rep coach note's AI-path Adaptation read (round 44 surface), the SOLVED-branch cross-surface contract (round 45), the TRAJECTORY + thin-just-finished cross-surface contract (round 46), the stalled-50% cross-surface contract (round 47), AND now the boundary completion (round 48) all have testable structural anchoring contracts.
- **Coach-parity stage #3 (Intervention) — "prescribe a drill for a reason, name the observable target, and define what improvement would look like before the user starts."** Round 48 pins the boundary failure modes the per-rep Intervention read could regress into: at 0%-stalled the recommendation engine prescribes a real drill (the scenario is squarely below the bar), but the per-rep post-rep note must stay on metric-chain language — a coaching-shaped trajectory clause on a flat-line miss reads as the coach has nothing useful to say; at 100%-stalled the steady-state on-brand competence is not a coaching moment ("you're still on-brand" reads as filler, not signal); at 50%-stalled + prior-crossing-C the temptation is to pivot to the old win in C, which would abandon the rep the user just produced. Round 48 makes all three pivots structurally impossible on both surfaces in lockstep.
- **Pillar #5 (Personalized coaching).** A coach (deterministic OR AI) who, after a fully-off-brand stalled rep, opens with a coaching-shaped trajectory clause reads as a coach who has nothing useful to say; after a fully-on-brand stalled rep, opens with "you're still on-brand" reads as a coach padding with filler; after a 50%-alternating stalled rep with an old win in another scenario, opens with "you've done it before" reads as a coach pivoting away from the rep in front of them. Round 48 makes per-rep anchoring structurally guaranteed on both paths in lockstep across all three boundary stalled cases. The user can't tell which path produced their note (the surface labels it `isAIBacked: true/false`), so the stalled-anchoring contract must hold on both at every rate point — and now is pinned by tests that drive both surfaces simultaneously across the full stalled gradient.
- **Pillar #4 (Believable progress).** A stalled trajectory is the moment the post-rep note is most tempted to invent narrative drama. At 0%-stalled the temptation is to manufacture momentum from the floor ("we're going to turn this around"); at 100%-stalled the temptation is to manufacture stretch from the ceiling ("here's the next thing"); at 50%-stalled + prior-crossing-C the temptation is to pull from another scenario's resolved arc ("the real story is over there"). All three are exactly the failure modes a real coach avoids — the right answer at every rate point is to read the per-rep metrics honestly. Round 48 makes all three fall-backs impossible on both surfaces in three methods, so a future regression on either path at any rate point is caught before it ships to the user.
- **Pillar #3 (Conversational intelligence).** The AI prompt's metric chain (filler, score, pace, etc.) is preserved verbatim. The IM tone-drill TRAJECTORY clause (line ~1339 in `PostRepCoachNoteService.swift`) and SOLVED clause (line ~1348) both gate on `direction != .stalled` and `imToneDrillResolved != nil` respectively; the deterministic note's branches 0 (SOLVED, line ~620) and 0d (TRAJECTORY, line ~655) gate identically. Round 48 makes the two surfaces' stalled reads at the 0% and 100% boundaries — plus the stalled+prior-crossing cross-scenario angle — testably inseparable.
- **Anti-overclaim.** No engine state mutation, no new freshness gate, no new constants, no schema bump, no migration, no production code change at all. The change is a no-op on the behavior side; the tests pin the cross-surface structural contract that was already true.
- **Engineering bans.** No placeholder logic. No dead toggles. No fragmented state. The three new tests READ only the existing `PracticeSession.id` + `PracticeSession.mode` + `PracticeSession.imConversationDetails.setup.scenario` + `PracticeSession.imConversationDetails.setup.targetTone` + `PracticeSession.imConversationDetails.actualTone` fields plus the existing `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)` helper output (via the round-44/45/46/47 `makeInput` factory). No new storage, no schema bump, no migration, no new view inputs. The existing `PostRepCoachNoteService.userPrompt(from:)`, `PostRepCoachNoteService.deterministicNote(input:)`, `PostRepCoachNoteService.metricSentence(for:persona:)`, `IMHistorySummary.toneDrillProgress(from:scenario:)`, `IMHistorySummary.toneDrillCrossing(...)`, `IMHistorySummary.toneDrillResolved(...)`, and `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)` paths are preserved verbatim. Pure test addition on pure-function surfaces.

### Branch + redesign-alignment notes

- All tracks land on `Redesign`, the redesign-lineage branch the rolling M24 deferred-slate work has been shipping on since round 11. The user brief explicitly calls this out: "ensure working on the redesign branch too (very important)." Round 48 preserves the round-by-round loop on the redesign lineage.
- Round 48 does not change any of the round-47 / round-46 / round-45 / ... / round-26 surfaces; every prior suite remains unchanged; the round-48 3 new `@Test` methods sit at the end of the existing `PostRepCoachNoteUserPromptAnchoringTests` suite. The round-47-extended `PostRepCoachNoteUserPromptAnchoringTests` (21 tests, now 24 with round 48's additions), round-43 `PostRepCoachNoteJustFinishedRepAnchoringTests` (14 tests), round-42 `ToneDrillTrajectoryFreshnessTests` (15 tests), round-41 `ToneDrillSolvedFreshnessTests` (11 tests), and every other suite are preserved verbatim.
- The existing `PostRepCoachNoteToneTrajectoryTests` (5 tests, including the deterministic-path stalled fall-through which round 48's boundary tests sit alongside) and `PostRepCoachNoteToneResolvedTests` are preserved verbatim — round 48 is purely additive on the test side, no fixture or production touch.

## Future moves

(Updated priority list — round 48 closed the stalled-corner boundary completion, the deferred moves #30 + #31 from round 47. The rest roll forward, plus new moves from round 48.)

1. **Peer Sudden Death scores via `FriendsManager`.** Still blocked on `PublicProfileSnapshot` schema work.
2. **`coachNoteRevealed` cleanup.** Still risky — animation chain interleaving with celebration timing. Worth a dedicated refactor pass with proper visual QA (and a real device).
3. **Visual polish pass on the round-19 launch CTA.** Carried forward from rounds 19–47. Pure visual work, not destination logic.
4. **Visual polish pass on the round-20 SOLVED ribbon.** Carried forward from rounds 20–47. Pure visual work, not crossing logic.
5. **Day-rollover refresh for long-mounted observers.** Carried forward from round 22.
6. **Tier-change observation symmetry to other surfaces that read `AIRateLimiter.currentCap()` directly.** Carried forward from round 23.
7. **Refresh-on-rotate for the empty-state chip when the `CoachMemoryStore` mutates while AskNoumView is mounted.** Carried forward from round 25.
8. **Voice-tuned ack-chip glyphs.** Carried forward from round 26.
9. **Engine-level case-anchored amplification (post-round-37 escalation).** Carried forward from round 37 as a future move. Restraint pin: hold until real-conversation evidence calls for engine-level escalation.
10. **Engine-level under-repeated-pushback dampening (post-round-38 escalation).** Carried forward from round 38. Restraint pin: hold for real-conversation evidence.
11. **Engine-level case-anchored continuation (post-round-39 escalation).** Carried forward from round 39. Restraint pin: hold for real-conversation evidence.
12. **Engine-level under-pushback continuation dampening (post-round-40 escalation).** Carried forward from round 40. Restraint pin: hold for real-conversation evidence.
13. **Collapse the round-26 hypothesis-ack reflection in `coachCaseFormulationLines` into a single block with the round-32 rebuild-verdict lines when the predicate fires.** Carried forward from round 32. Hold for real-device QA.
14. **Trend-view distinction across the rebuild-verdict / case-state matrix surfaces.** Carried forward from rounds 30–47. Pure visual work; hold for real-device QA.
15. **Second-cycle ask register on the opener.** Note from round 35. Restraint pin: hold until real conversations show the cycle-agnostic ask reads as off.
16. **`CaseReviewCard` adaptation-log fuller history surface.** Note from round 36. Hold for real-device QA.
17. **`CaseReviewCard` case-anchored badge.** Note from round 37. Hold for real-device QA.
18. **`CaseReviewCard` under-pushback badge.** Note from round 38. Hold for real-device QA.
19. **`CaseReviewCard` case-anchored continuation badge.** Note from round 39. Hold for real-device QA.
20. **`CaseReviewCard` under-pushback continuation badge.** Note from round 40. Hold for real-device QA.
21. **Apply the freshness window to the `IMToneDrillProgress.stalled` chat-coach read separately from `recovering`/`slipping`.** Carried forward from round 42. Restraint pin: hold for real-conversation evidence of the stalled-and-stuck dynamic before splitting the window. (Note: this is the chat-coach freshness gate, distinct from the round-47/48 stalled-anchoring contract on the post-rep note surfaces. Both surfaces honor stalled by gating it out; the chat-coach surface uses a freshness gate, the post-rep note surfaces use the per-rep helper anchoring.)
22. **Lift the recent-summaries + recent-proofs construction into `MomentumComputer` (or a sibling pure helper) for parallel anchoring tests.** Carried forward from round 43. The finalizer's `recentSessionSummaries` and `recentProofQuotes` are constructed inline in `recordPostRepCoachNote` (the same shape as the pre-round-43 IM tone-drill block). They DON'T have the same cross-rep cold-open concern (they're explicitly cross-rep summaries by design), but lifting them into pure helpers would make the per-rep finalizer wire more uniformly testable. Pure refactor; no behavior change; hold for a slow round.
23. **Defensive pin on `PostRepCoachNoteStore.record(_:)` dedupe-by-sessionID contract.** Carried forward from round 43. The store de-duplicates notes by sessionID (the finalize → AI upgrade → re-record path relies on this); a defensive store-level test pinning the dedupe contract and the "never-write-for-deleted-session" guard would lock the surface symmetric to the round-43 helper. Hold for the right round.
24. **Unify the round-43 + round-44 fixture helpers (`imSession`, `nonIMSession`, `slippingScenarioSessions`, `crossingScenarioSessions`) into one private `IMHelpers` namespace shared across both suites.** Carried forward from round 44. The round-44/45/46/47/48 suites reuse the round-43 helpers via redeclaration; the round-46 + round-47 + round-48 tests build the recovering / stalled / 0%-stalled / 100%-stalled fixtures inline. A future refactor lifts them into a shared namespace so a future test author doesn't need to keep five+ helpers in sync. Round 48 adds two more candidates to the namespace: `fullyOffBrandStalledScenarioSessions(scenario:targetTone:offBrandTone:earliestDayOffset:)` and `fullyOnBrandStalledScenarioSessions(scenario:targetTone:onBrandTone:earliestDayOffset:)` helpers currently built inline in round 48's tests 2 and 3. Pure test refactor; no behavior change; hold for a slow round.
25. **AI-path tests for the metric branches (filler, score, pace) routed through `userPrompt(from:)`.** Carried forward from round 44. The round-43 + round-44 + round-45 + round-46 + round-47 + round-48 contract is locked on the IM tone-drill clauses; the per-rep metric branches (filler comparison vs baseline, zero-fillers, score bands, etc.) construct prompt lines from the same `PostRepCoachNoteInput` fields. A defensive suite mirroring round-44's shape (12 tests, fixture-driven, route through `userPrompt(from:)`) would extend the same anchoring contract surface to the metric branches. Hold for the right round.
26. **Cross-surface contract for the consecutive-clean-reps / personal-best / filler-trend momentum lines.** Note from round 45. The same `userPromptAndDeterministicNote*` pattern that locks the IM tone-drill clauses on both surfaces could extend to the non-IM momentum lines — consecutive clean reps (line ~1322 in `userPrompt`), personal best (~1325), filler trend (~1330), score trend (~1333), weekly rep count (~1336). The deterministic priority chain reads these in branches 0a–0d (line ~627-662 in `metricSentence`). A symmetric cross-surface suite (one test per momentum branch) would pin both paths in lockstep. Hold for the right round.
27. **End-to-end cross-surface contract that routes through `PracticeSessionFinalizer.recordPostRepCoachNote`.** Note from round 45. The current cross-surface contracts (round 44 TRAJECTORY + round 45 SOLVED + round 46 thin-just-finished + round 47 stalled + round 48 boundary completion) route through the pure `userPrompt(from:)` + `deterministicNote(input:)` surfaces directly, bypassing the finalizer wire. A defensive end-to-end test that constructs a `PracticeSession` set, invokes the finalizer, and asserts the persisted `PostRepCoachNote.noteText` headlines the just-finished rep's scenario (or stays silent when thin / stalled at any rate) would lock the FULL wire (finalizer → helper → input → deterministic-or-AI note). Higher complexity (the finalizer takes a `CoachMemoryStore`, `PracticeSessionsStore`, etc.); hold for a build-host-equipped round.
28. **Mixed-just-finished thin + own-scenario crossing-elsewhere-in-history cross-surface contract.** Note from round 46. The trickiest cold-open is: just-finished rep is thin (one rep in A), but the SAME scenario A had a CROSSING rep weeks ago that's now stale. The helper's `toneDrillCrossing(in:scenario:currentRepId:)` is anchored on `currentRepId`, so an old crossing in the same scenario but on a different rep is invisible to the just-finished rep's read — by construction. A defensive cross-surface test would pin this on both surfaces. Hold for the right round.
29. **Helper-output assertion layered into the cross-surface tests.** Note from round 46. The round-46 + round-47 + round-48 baseline tests assert the *prompt + note text* don't name the parallel scenario, which is the user-facing contract. A defensive belt would also assert at the helper output: `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:).progress == nil && .resolved == nil` for the thin-A justFinished (round 46), `.progress?.direction == .stalled && .resolved == nil` for the stalled-A justFinished (rounds 47 + 48), AND `.progress?.earlierRate == 0.0` / `.progress?.earlierRate == 1.0` for the round-48 boundary fixtures. The single-surface round-43 test `helperReturnsNilProgressOnThinJustFinishedRep` covers the thin case on the deterministic suite; lifting the assertion into the cross-surface suite would catch a helper regression independent of the surface tests. Hold for the right round. **Was move #29 in round 47; round 48 extends the assertion shape to cover the boundary fixtures.**
30. **Helper-level test that `toneDrillProgress` returns `direction == .stalled` for the round-47 + round-48 fixtures.** Carried forward from round 47 as move #32. The round-47/48 cross-surface tests assert the surface outputs don't contain "tone-match"; a helper-level test asserting `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:).progress?.direction == .stalled` for the same fixtures (50%-alternating, 0%-all-off, 100%-all-on) would catch a helper regression that shifted the stalled boundary silently (e.g., the threshold drifts to 0.05 and a fixture starts reading as recovering or slipping). Defensive belt; hold for the right round.
31. **Stalled-trajectory window-size = 2 vs window-size = 3 cross-surface contract.** New note from round 48. The round-47/48 stalled fixtures all use 4 evaluated reps (`window = min(3, 4/2) = 2`). The next granularity is 6+ reps (`window = min(3, 6/2) = 3`), which exercises the wider-window stalled gate. A defensive cross-surface test on a 6-rep stalled fixture (window=3) would extend the contract to the next window-size step. Hold for the right round.
32. **Stalled-trajectory at the boundary of `toneDrillProgressThreshold`.** New note from round 48. The round-47/48 stalled fixtures have `delta == 0` (the cleanest stalled signal). A defensive cross-surface test at the exact boundary (`delta == ±toneDrillProgressThreshold - ε`) would lock the gate on its tightest discriminator. Hold for the right round.
33. **Stalled-A + parallel-thin-B cross-surface contract.** New note from round 48. Round 47 covered stalled-A × (own / slipping-B / recovering-B); round 48 covered stalled-A × (own boundaries + prior-crossing-C). The remaining cross-scenario angle is stalled-A + thin-B (the parallel scenario has only 1-3 reps — no qualifying trajectory but also no SOLVED win). The helper threads only A through, so B's thinness is invisible to both surfaces — a symmetric companion to round 46's thin-A × parallel-loud tests. Defensive companion; hold for the right round.

## Build-host limitation (honest note for the next agent)

This environment has **no Xcode and no Swift toolchain**, so nothing in this round was compiled or run — not the app, not the test suite. The change is:

- Three new `@Test` methods (`userPromptAndDeterministicNoteStalledJustFinishedNeverNameParallelPriorCrossing`, `userPromptAndDeterministicNoteFullyOffBrandStalledJustFinishedNeverProducesOwnTrajectoryClause`, `userPromptAndDeterministicNoteFullyOnBrandStalledJustFinishedNeverProducesOwnTrajectoryClause`) appended to the existing `PostRepCoachNoteUserPromptAnchoringTests` suite in `NoumTests/NoumTests.swift`. Slotted immediately after the existing round-47 `userPromptAndDeterministicNoteStalledJustFinishedNeverNameParallelRecoveringScenario` test and immediately before the suite's closing brace. The stalled-A 50%-alternating fixture in test 1 mirrors round 47's shape verbatim; the 0%-all-off and 100%-all-on fixtures in tests 2 and 3 are built inline (4-rep all-same-tone shapes, the natural rate-extreme companions to round 47's 50%-alternating baseline); the parallel prior-crossing-C fixture in test 1 reuses the existing `crossingScenarioSessions` helper verbatim.

All checks the next agent should run on a real build host:

1. `swift test --filter PostRepCoachNoteUserPromptAnchoringTests` — the suite now has 24 tests (round-44's 12 + round-45's 3 + round-46's 3 + round-47's 3 + round-48's 3); all should pass.
2. `swift test --filter PostRepCoachNoteJustFinishedRepAnchoringTests` — round-43's 14 tests should still pass verbatim (the round-48 additions are scoped to the round-44/45/46/47 suite).
3. `swift test --filter PostRepCoachNoteToneTrajectoryTests` — round-15's existing tests (including the deterministic-path stalled fall-through) should still pass verbatim.
4. `swift test --filter PostRepCoachNoteToneResolvedTests` — the existing SOLVED-branch tests should still pass verbatim.
5. `swift test --filter ToneDrillTrajectoryFreshnessTests` — round-42's 15 tests should still pass verbatim.
6. `swift test --filter ToneDrillSolvedFreshnessTests` — round-41's 11 tests should still pass verbatim.
7. `swift test --filter ToneDrillTrajectoryContextTests` — round-12's 8 tests should still pass.
8. `swift test --filter ToneDrillResolvedContextTests` — round-13's 4 tests should still pass.
9. `swift test --filter IMToneDrillSignalTests` — round-12/14's primitive tests should still pass.
10. `swift test --filter InterventionUnderPushbackContinuationTests` — round-40's 29 tests should still pass.
11. `swift test --filter CaseAnchoredContinuationTests` — round-39's 28 tests should still pass.
12. `swift test --filter CaseAnchoredAmplificationTests` — round-37's 27 tests should still pass.
13. `swift test --filter InterventionUnderRepeatedPushbackTests` — round-38's 32 tests should still pass.
14. `swift test --filter RebuildVerdictContextTests` — round-32's 25 tests should still pass.
15. `swift test --filter FreshRevisedReadContextTests` — round-31's 16 tests should still pass.
16. Boot the app on simulator, finish an IM rep in scenario A (e.g. Networking, confident) where the user has been consistently off-brand for 4+ reps (0%-stalled — the floor case) while having parallel signals elsewhere. Confirm the post-rep Coach's Read card on the Summary screen speaks to scenario A's per-rep metrics (filler, score, pace, weekly rep count) — NEVER emits a "tone-match" clause referring to A's flat-line miss, and NEVER mentions the parallel scenario's title. The card should fall through to a metric-chain read.
17. Same test as step 16, but with the user being consistently on-brand for 4+ reps (100%-stalled — the ceiling case). The card should still stay on metric-chain language — "you're still on-brand" is not coaching language and should not appear.
18. Same test as step 16, but with the user 50%-alternating in scenario A AND having a prior crossing rep in scenario C (Work Update, professional) from weeks ago. The card should stay on A's metrics — the old win in C should not headline (the cross-rep cold-open is structurally impossible).
19. With the AI provider configured (when available), repeat steps 16–18 and confirm the AI-backed note also stays on scenario A's metrics across all three boundary cases.
20. Confirm the round-21 SOLVED ribbon on the hero score card does NOT fire on any of the three round-48 stalled boundary cases — same primitive (`toneDrillCrossing` → `toneDrillResolved`) routes both surfaces through the helper, so the two surfaces remain in lockstep with the ribbon.
21. Confirm the round-26 hypothesis-ack chip, round-32 rebuild-verdict line, round-37/38/39/40 case-state matrix, round-31 fresh-revised-read context block, round-41 SOLVED freshness window, round-42 TRAJECTORY freshness window, round-43 deterministic-path anchoring, round-44 AI-path anchoring, round-45 cross-surface SOLVED contract, round-46 cross-surface thin-just-finished contract, round-47 cross-surface stalled-just-finished contract, and round-48 cross-surface stalled-just-finished boundary completion contract are all preserved verbatim.
