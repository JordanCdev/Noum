# HANDOFF — M24 deferred slate (round 50): helper-level direction + boundary-rate assertions on the round-44/45 recovering / slipping / crossing fixtures — three new `@Test` methods drive each non-stalled fixture (recovering-A `[tense, rushed, tense, calm]`, slipping-B `[confident, confident, shaky, shaky]`, crossing-A `[tense, tense, calm, calm]` with the just-finished rep being the crossing rep) through `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)` and assert the helper output directly. Recovering: `.progress?.direction == .recovering`, `earlierRate == 0.0`, `recentRate == 0.5`, `windowSize == 2`, `resolved == nil` (hold-rate arm gates off — 0.5 NOT >= 0.6), titles "Difficult Conversation" / "Calm". Slipping: `.progress?.direction == .slipping`, `earlierRate == 1.0`, `recentRate == 0.0`, `windowSize == 2`, `resolved == nil` (earlier-below-bar arm gates off — 1.0 NOT < 0.4), titles "Networking" / "Confident". Crossing: `.progress?.direction == .recovering`, `earlierRate == 0.0`, `recentRate == 1.0`, `windowSize == 2`, `resolved != nil`, `resolved?.earlierRate == 0.0`, `resolved?.recentRate == 1.0`, `resolved?.scenario == .difficultConversation`, `resolved?.targetTone == .calm`, `resolvedScenarioTitle == "Difficult Conversation"`, `resolvedToneTitle == "Calm"`, title equality across progress + resolved sides — both anchored on the just-finished rep's scenario, the round-43 contract pinned again at the rate-boundary fixture. Round 50 closes future move #33 from round 49's HANDOFF — the symmetric companion to round 49's helper-boundary belt on the stalled fixtures — and completes the two-layer regression net (surface + helper) on all four corners of the 2×2 direction × locus matrix (recovering / slipping / crossing / stalled). Pure test addition; no production code change; no engine state change; no schema bump; no migration; no new view inputs.

## Scope

Round 49 closed future move #30 from round 48's HANDOFF (helper-level direction assertions on the round-47/48 stalled fixtures) and partially closed move #29 (helper-output assertion on the round-48 boundary fixture legs). The remaining gaps it explicitly carried forward as future moves:

- **Future move #33** (round 49 HANDOFF): "Helper-boundary assertion on the recovering / slipping / crossing fixtures. Round 49 covers the helper-boundary assertion for the three stalled fixtures (50%-alternating, 0%-all-off, 100%-all-on). The symmetric companion would assert the helper output for the round-44 recovering fixture (`.progress?.direction == .recovering`, exact rate values, `windowSize`), the round-44 slipping fixture (`.progress?.direction == .slipping`, exact rate values), and the round-45 crossing fixture (`.resolved != nil`, `resolvedScenarioTitle == "Difficult Conversation"`, the `earlierRate` / `recentRate` values on the crossing). Defensive belt extending the round-49 pattern to the non-stalled corners of the matrix."
- **Future move #29** (remaining leg): the thin-A round-46 leg of `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:).progress == nil && .resolved == nil`. Still deferred for a future round.
- **Future move #30** (round 49 numbering): stalled-trajectory window-size = 2 vs window-size = 3 cross-surface contract. Defensive companion; hold for the right round.
- **Future move #31** (round 49 numbering): stalled-trajectory at the boundary of `toneDrillProgressThreshold`. Defensive companion; hold for the right round.
- **Future move #32** (round 49 numbering): stalled-A + parallel-thin-B cross-surface contract. Defensive companion; hold for the right round.
- **Future move #34** (round 49 numbering): helper-boundary assertion on the parallel-loud fixtures (slipping-B / recovering-B / prior-crossing-C / thin-B) — the helper-boundary "anchored on A only" contract.

Round 50 closes #33 head-on. Three new `@Test` methods on `PostRepCoachNoteUserPromptAnchoringTests`, slotted at the end of the suite immediately after the round-49 `helperReturnsStalledDirectionForFullyOnBrand100PercentStalledFixture` test and immediately before the closing brace, under a new `// MARK: - Helper-level direction assertions on the round-44/45 recovering / slipping / crossing fixtures (round 50)` section:

1. **`helperReturnsRecoveringDirectionForRoundFortyFourRecoveringFixture`** — recovering-A own-scenario. Actual tones in scenario A (Difficult Conversation, calm): `["tense", "rushed", "tense", "calm"]` → outcomes `[false, false, false, true]`; earlier prefix `[false, false]` = 0%; recent suffix `[false, true]` = 50%; delta = 0.5 → recovering. SOLVED hold-rate arm gates off (0.5 NOT >= 0.6). Helper drives `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)` and asserts: `progress?.direction == .recovering`, `progress?.earlierRate == 0.0`, `progress?.recentRate == 0.5`, `progress?.windowSize == 2`, `resolved == nil`, `resolvedScenarioTitle == nil`, `resolvedToneTitle == nil`, `scenarioTitle == "Difficult Conversation"`, `toneTitle == "Calm"`.
2. **`helperReturnsSlippingDirectionForRoundFortyFiveSlippingFixture`** — slipping-B own-scenario. Actual tones in scenario B (Networking, confident): `["confident", "confident", "shaky", "shaky"]` → outcomes `[true, true, false, false]`; earlier prefix `[true, true]` = 100%; recent suffix `[false, false]` = 0%; delta = -1.0 → slipping. SOLVED earlier-below-bar arm gates off (1.0 NOT < 0.4). Asserts: `progress?.direction == .slipping`, `progress?.earlierRate == 1.0`, `progress?.recentRate == 0.0`, `progress?.windowSize == 2`, `resolved == nil`, `resolvedScenarioTitle == nil`, `resolvedToneTitle == nil`, `scenarioTitle == "Networking"`, `toneTitle == "Confident"`.
3. **`helperReturnsResolvedAndRecoveringWithBoundaryRatesForRoundFortyFiveCrossingFixture`** — crossing-A own-scenario. Built via the suite's `crossingScenarioSessions` helper with `scenario: .difficultConversation`, `targetTone: .calm`, `offBrandTone: "tense"`, `onBrandTone: "calm"`, `crossingRepId: crossingId`, `earliestDayOffset: -3`. Outcomes `[false, false, true, true]`; earlier `[false, false]` = 0%; recent `[true, true]` = 100%; delta = 1.0 → recovering. SOLVED gate passes (0.0 < 0.4 AND 1.0 >= 0.6 AND overallRate 0.5 >= 0.4) AND the just-finished rep IS the crossing rep (`justFinished.id == crossingId`). Defensive belt asserts `justFinished.id == crossingId` first (suite-helper wiring check). Then asserts: `progress?.direction == .recovering`, `progress?.earlierRate == 0.0`, `progress?.recentRate == 1.0`, `progress?.windowSize == 2`, `resolved != nil`, `resolved?.earlierRate == 0.0`, `resolved?.recentRate == 1.0`, `resolved?.scenario == .difficultConversation`, `resolved?.targetTone == .calm`, `resolvedScenarioTitle == "Difficult Conversation"`, `resolvedToneTitle == "Calm"`, `scenarioTitle == "Difficult Conversation"`, `toneTitle == "Calm"`, `resolvedScenarioTitle == scenarioTitle`, `resolvedToneTitle == toneTitle`.

The three tests together pin the helper-level recovering / slipping / crossing contract directly at the helper output for each fixture shape the round-44/45 cross-surface tests drive end-to-end through the prompt + deterministic note surfaces. A future regression that broke ONLY the direction classification (e.g., a threshold sign flip) or the boundary-rate arithmetic (e.g., a window-split off-by-one) or the resolved object's rate copying (e.g., `recentRate: progress.earlierRate` by mistake), without breaking the surface templates' anchoring titles, would still be caught at the helper boundary before either surface silently swallowed the change. Round 49 (stalled) + round 50 (recovering / slipping / crossing) together complete the helper-boundary defensive belt on all four corners of the 2×2 direction × locus matrix, in lockstep with the surface tests on rounds 44 / 45 / 46 / 47 / 48.

Defensive scoping (intentional restraint, same shape as rounds 41/42/43/44/45/46/47/48/49):

- **Pure test addition, no production code change.** Round 50 modifies only `NoumTests/NoumTests.swift` and `HANDOFF.md`. Every production surface (`MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)`, `PostRepCoachNoteService.userPrompt(from:)`, `PostRepCoachNoteService.deterministicNote(input:)`, `IMHistorySummary.toneDrillProgress(from:scenario:)`, `IMHistorySummary.toneDrillCrossing(...)`, `IMHistorySummary.toneDrillResolved(...)`) is preserved verbatim. The helper-level contract round 50 pins was already true at rounds 12/14 (the `IMHistorySummary.toneDrillProgress` primitive's recovering/slipping gates via `delta >= toneDrillProgressThreshold` / `delta <= -toneDrillProgressThreshold`) and at round 43 (the helper threads only the just-finished rep's scenario through both `toneDrillProgress` and `toneDrillCrossing`); round 50 locks it at the helper output (`MomentumComputer.imToneDrillNoteFields(...)`) for each round-44/45 fixture shape.
- **No new freshness gate.** Same rationale as rounds 43/44/45/46/47/48/49: the post-rep note is written EXACTLY ONCE, immediately after the rep finalizes, against signals computed strictly for the just-finished rep's scenario. The per-rep anchoring IS the freshness contract on both the deterministic and AI paths. Round 50 does NOT introduce a recency window.
- **No engine state change.** All `IMHistorySummary` helpers, `MomentumComputer.imToneDrillNoteFields`, both surface bodies — unchanged. No new persisted state, no schema bump, no migration, no new view inputs.
- **`MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)` is `static`.** Round 50 drives the helper directly without finalizer plumbing — the same testability lever rounds 43–49 used. No mock provider, no finalizer wire, no rate-limit gymnastics.
- **Test fixtures mirror rounds 44/45 verbatim.** The recovering-A fixture uses the same `[tense, rushed, tense, calm]` shape as `userPromptIncludesTrajectoryClauseNamingJustFinishedRepsScenario` (round 44) and `userPromptAndDeterministicNoteNameSameJustFinishedRepsScenario` (round 45's TRAJECTORY-branch cross-surface contract). The slipping-B fixture uses the same `[confident, confident, shaky, shaky]` inline shape as `userPromptAndDeterministicNoteSolvedBranchHoldsWhenParallelScenarioIsRecentlySlipping` (round 45). The crossing-A fixture uses the suite's `crossingScenarioSessions` helper unchanged, the same helper round 43's `helperPopulatesResolvedTrioOnCrossingRep` test and round 45's `userPromptAndDeterministicNoteSolvedBranchAnchorOnJustFinishedScenario` test drive end-to-end through the surfaces. No new fixture helper.
- **Tests pin BOTH the direction classification AND the boundary rate values AND (for the crossing fixture) the resolved object's internal rate boundaries.** Three independent failure modes pinned per fixture (direction, rate-window split, and — on the crossing — resolved object's rate-copying wiring). A regression that broke ONLY one trips the corresponding assertion independently of the others.
- **Each test drives the helper ONLY.** No prompt assertion, no deterministic note assertion — the round-44/45 cross-surface tests already pin those. Round 50 is purely the helper-boundary defensive belt; the surface tests + the helper-boundary tests together form a two-layer regression net.
- **The crossing test layers the suite-helper wiring check first.** The `#expect(justFinished.id == crossingId)` assertion catches a future refactor of `crossingScenarioSessions` that flipped the `crossingRepId` parameter wiring (e.g., wiring it to the 3rd rep instead of the 4th); without that check, a wiring inversion would silently change which rep `toneDrillCrossing` evaluates against and the resolved-trio assertions could pass coincidentally.

User brief, unchanged round to round: "continue from the existing TO-DO, ensure working towards getting the app towards the vision plan, and all round A+, make my dream I had come true too, ensure working on the redesign branch too (very important)."

Translation, this round:

- Three new `@Test` methods (`helperReturnsRecoveringDirectionForRoundFortyFourRecoveringFixture`, `helperReturnsSlippingDirectionForRoundFortyFiveSlippingFixture`, `helperReturnsResolvedAndRecoveringWithBoundaryRatesForRoundFortyFiveCrossingFixture`) appended to the existing `PostRepCoachNoteUserPromptAnchoringTests` suite, slotted immediately after round 49's `helperReturnsStalledDirectionForFullyOnBrand100PercentStalledFixture` test under a new `// MARK: - Helper-level direction assertions on the round-44/45 recovering / slipping / crossing fixtures (round 50)` section so the recovering / slipping / crossing / thin / stalled cross-surface contracts AND the helper-boundary defensive belt read as one contiguous block. No new fixtures.
- The redesign-branch invariant: round 50 is a `Redesign`-branch push per the user brief. It preserves the round-by-round loop on the redesign lineage that has been the home of rounds 11–49.

## What shipped

### Track 1 — Three helper-level direction + boundary-rate assertion tests on the round-44/45 recovering / slipping / crossing fixtures (`NoumTests/NoumTests.swift`)

Three new `@Test` methods on the existing `struct PostRepCoachNoteUserPromptAnchoringTests` suite (the round-44/45/46/47/48/49 suite, now 30 tests), slotted under a new `// MARK: - Helper-level direction assertions on the round-44/45 recovering / slipping / crossing fixtures (round 50)` section immediately after the existing round-49 `helperReturnsStalledDirectionForFullyOnBrand100PercentStalledFixture` test and immediately before the closing brace of the suite. The recovering-A and slipping-B fixtures are built inline (matching the round-44/45 surface tests' shapes verbatim); the crossing-A fixture uses the suite's `crossingScenarioSessions` helper unchanged.

- **`helperReturnsRecoveringDirectionForRoundFortyFourRecoveringFixture`** — recovering-A own-scenario. Just-finished rep is the 4th rep in scenario A (Difficult Conversation, calm) with `[tense, rushed, tense, calm]` (the round-44 baseline fixture). Drives the helper directly and asserts: `fields.progress?.direction == .recovering`, `fields.progress?.earlierRate == 0.0`, `fields.progress?.recentRate == 0.5`, `fields.progress?.windowSize == 2`, `fields.resolved == nil`, `fields.resolvedScenarioTitle == nil`, `fields.resolvedToneTitle == nil`, `fields.scenarioTitle == "Difficult Conversation"`, `fields.toneTitle == "Calm"`.
- **`helperReturnsSlippingDirectionForRoundFortyFiveSlippingFixture`** — slipping-B own-scenario. Just-finished rep is the 4th rep in scenario B (Networking, confident) with `[confident, confident, shaky, shaky]` (the round-45 inline `slippingB` fixture). Drives the helper directly and asserts: `fields.progress?.direction == .slipping`, `fields.progress?.earlierRate == 1.0`, `fields.progress?.recentRate == 0.0`, `fields.progress?.windowSize == 2`, `fields.resolved == nil`, `fields.resolvedScenarioTitle == nil`, `fields.resolvedToneTitle == nil`, `fields.scenarioTitle == "Networking"`, `fields.toneTitle == "Confident"`.
- **`helperReturnsResolvedAndRecoveringWithBoundaryRatesForRoundFortyFiveCrossingFixture`** — crossing-A own-scenario. Built via the suite's `crossingScenarioSessions` helper with `scenario: .difficultConversation`, `targetTone: .calm`, `offBrandTone: "tense"`, `onBrandTone: "calm"`, `crossingRepId: crossingId`, `earliestDayOffset: -3`. Just-finished is the 4th rep (the crossing rep). Layers a defensive `#expect(justFinished.id == crossingId)` wiring check first. Asserts: `fields.progress?.direction == .recovering`, `fields.progress?.earlierRate == 0.0`, `fields.progress?.recentRate == 1.0`, `fields.progress?.windowSize == 2`, `fields.resolved != nil`, `fields.resolved?.earlierRate == 0.0`, `fields.resolved?.recentRate == 1.0`, `fields.resolved?.scenario == .difficultConversation`, `fields.resolved?.targetTone == .calm`, `fields.resolvedScenarioTitle == "Difficult Conversation"`, `fields.resolvedToneTitle == "Calm"`, `fields.scenarioTitle == "Difficult Conversation"`, `fields.toneTitle == "Calm"`, `fields.resolvedScenarioTitle == fields.scenarioTitle`, `fields.resolvedToneTitle == fields.toneTitle`.

### Vision alignment

- **Coach-parity stage #4 (Adaptation) — "compare response across multiple attempts and either reinforce, vary, or replace the intervention with an explained rationale."** Per `docs/VISION.md`. Round 50 completes the helper-boundary belt round 49 started: rounds 44 + 45 + 46 + 47 + 48 pin the surface outputs (prompt + deterministic note) across the full 2×2 direction × locus matrix (recovering / slipping / crossing / thin / stalled); round 49 pins the helper-level direction classification at the stalled corner; round 50 pins the helper-level direction + boundary-rate contract at the recovering / slipping / crossing corners. The two-layer regression net (surface + helper) is now structurally complete on all four corners of the matrix — a future regression on either layer trips at least one test independently of which corner of the matrix produced the just-finished rep's read.
- **Coach-parity stage #3 (Intervention) — "prescribe a drill for a reason, name the observable target, and define what improvement would look like before the user starts."** Round 50 pins the helper-level contract the per-rep Intervention read depends on at the recovering / slipping / crossing corners: at recovering the per-rep note reads "the climb is real" and the surface trajectory clause fires; at slipping the per-rep note reads "you were on-brand and you're losing it" and the slipping surface trajectory clause fires; at crossing the SOLVED clause headlines the win once. The helper's direction classification + boundary rates are what guarantee both surface gates fire in lockstep with the right intervention voice at each corner, and round 50 pins them at the helper boundary at all three non-stalled corners.
- **Pillar #5 (Personalized coaching).** A coach (deterministic OR AI) who, after a recovering rep, opens with a slipping-shaped trajectory clause reads as a coach who hasn't read the climb in front of them. Round 50 pins the helper-level direction classification that both surfaces depend on for their recovering / slipping / crossing gates, so a future regression that flipped the threshold sign or broke the window-split arithmetic is caught at the helper output before either surface silently produces a wrong-direction read.
- **Pillar #4 (Believable progress).** The recovering trajectory is the moment the post-rep note is most tempted to call the climb a SOLVED win (it isn't yet — the hold-rate arm gates off). The slipping trajectory is the moment the note is most tempted to call the loss a SOLVED collapse (it isn't — the resolved primitive only fires on positive turnarounds). The crossing rep is the single moment the SOLVED clause IS the right read. Round 50 pins the helper-level boundaries that distinguish the three reads, so the note headlines exactly the right voice at each corner.
- **Pillar #3 (Conversational intelligence).** The IM tone-drill TRAJECTORY clause (line ~1339 in `PostRepCoachNoteService.swift`) and SOLVED clause (line ~1348) both read from `PostRepCoachNoteInput.imToneDrillProgress` and `imToneDrillResolved`, which the finalizer threads through from `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)`. Round 50 makes the helper boundary's recovering / slipping / crossing-fixture output testably stable across all three non-stalled corners, completing the lockstep contract round 49 started at the stalled corner.
- **Anti-overclaim.** No engine state mutation, no new freshness gate, no new constants, no schema bump, no migration, no production code change at all. The change is a no-op on the behavior side; the tests pin the helper-level recovering / slipping / crossing boundaries that were already true at the primitive (`IMHistorySummary.toneDrillProgress`, `IMHistorySummary.toneDrillResolved`).
- **Engineering bans.** No placeholder logic. No dead toggles. No fragmented state. The three new tests READ only the existing `PracticeSession.id` + `PracticeSession.mode` + `PracticeSession.imConversationDetails.setup.scenario` + `PracticeSession.imConversationDetails.setup.targetTone` + `PracticeSession.imConversationDetails.actualTone` fields plus the existing `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)` helper output. No new storage, no schema bump, no migration, no new view inputs. The existing helper + primitive paths are preserved verbatim. Pure test addition on a pure-function surface.

### Branch + redesign-alignment notes

- All tracks land on `Redesign`, the redesign-lineage branch the rolling M24 deferred-slate work has been shipping on since round 11. The user brief explicitly calls this out: "ensure working on the redesign branch too (very important)." Round 50 preserves the round-by-round loop on the redesign lineage.
- Round 50 does not change any of the round-49 / round-48 / round-47 / ... / round-26 surfaces; every prior suite remains unchanged; the round-50 3 new `@Test` methods sit at the end of the existing `PostRepCoachNoteUserPromptAnchoringTests` suite under a new MARK section. The round-49-extended `PostRepCoachNoteUserPromptAnchoringTests` (27 tests, now 30 with round 50's additions), round-43 `PostRepCoachNoteJustFinishedRepAnchoringTests` (14 tests), round-42 `ToneDrillTrajectoryFreshnessTests` (15 tests), round-41 `ToneDrillSolvedFreshnessTests` (11 tests), and every other suite are preserved verbatim.
- The existing `PostRepCoachNoteToneTrajectoryTests` (5 tests, including the deterministic-path stalled fall-through which round 49/50's helper-boundary tests sit alongside) and `PostRepCoachNoteToneResolvedTests` are preserved verbatim — round 50 is purely additive on the test side, no fixture or production touch.

## Future moves

(Updated priority list — round 50 closed future move #33 from round 49's HANDOFF entirely. The rest roll forward, plus new moves from round 50.)

1. **Peer Sudden Death scores via `FriendsManager`.** Still blocked on `PublicProfileSnapshot` schema work.
2. **`coachNoteRevealed` cleanup.** Still risky — animation chain interleaving with celebration timing. Worth a dedicated refactor pass with proper visual QA (and a real device).
3. **Visual polish pass on the round-19 launch CTA.** Carried forward from rounds 19–49.
4. **Visual polish pass on the round-20 SOLVED ribbon.** Carried forward from rounds 20–49.
5. **Day-rollover refresh for long-mounted observers.** Carried forward from round 22.
6. **Tier-change observation symmetry to other surfaces that read `AIRateLimiter.currentCap()` directly.** Carried forward from round 23.
7. **Refresh-on-rotate for the empty-state chip when the `CoachMemoryStore` mutates while AskNoumView is mounted.** Carried forward from round 25.
8. **Voice-tuned ack-chip glyphs.** Carried forward from round 26.
9. **Engine-level case-anchored amplification (post-round-37 escalation).** Carried forward from round 37 as a future move. Restraint pin: hold until real-conversation evidence calls for engine-level escalation.
10. **Engine-level under-repeated-pushback dampening (post-round-38 escalation).** Carried forward from round 38. Restraint pin: hold for real-conversation evidence.
11. **Engine-level case-anchored continuation (post-round-39 escalation).** Carried forward from round 39. Restraint pin: hold for real-conversation evidence.
12. **Engine-level under-pushback continuation dampening (post-round-40 escalation).** Carried forward from round 40. Restraint pin: hold for real-conversation evidence.
13. **Collapse the round-26 hypothesis-ack reflection in `coachCaseFormulationLines` into a single block with the round-32 rebuild-verdict lines when the predicate fires.** Carried forward from round 32. Hold for real-device QA.
14. **Trend-view distinction across the rebuild-verdict / case-state matrix surfaces.** Carried forward from rounds 30–49. Pure visual work; hold for real-device QA.
15. **Second-cycle ask register on the opener.** Note from round 35. Restraint pin: hold until real conversations show the cycle-agnostic ask reads as off.
16. **`CaseReviewCard` adaptation-log fuller history surface.** Note from round 36. Hold for real-device QA.
17. **`CaseReviewCard` case-anchored badge.** Note from round 37. Hold for real-device QA.
18. **`CaseReviewCard` under-pushback badge.** Note from round 38. Hold for real-device QA.
19. **`CaseReviewCard` case-anchored continuation badge.** Note from round 39. Hold for real-device QA.
20. **`CaseReviewCard` under-pushback continuation badge.** Note from round 40. Hold for real-device QA.
21. **Apply the freshness window to the `IMToneDrillProgress.stalled` chat-coach read separately from `recovering`/`slipping`.** Carried forward from round 42. Restraint pin: hold for real-conversation evidence of the stalled-and-stuck dynamic before splitting the window.
22. **Lift the recent-summaries + recent-proofs construction into `MomentumComputer` (or a sibling pure helper) for parallel anchoring tests.** Carried forward from round 43. Pure refactor; no behavior change; hold for a slow round.
23. **Defensive pin on `PostRepCoachNoteStore.record(_:)` dedupe-by-sessionID contract.** Carried forward from round 43. Hold for the right round.
24. **Unify the round-43 + round-44 fixture helpers (`imSession`, `nonIMSession`, `slippingScenarioSessions`, `crossingScenarioSessions`) into one private `IMHelpers` namespace shared across both suites.** Carried forward from round 44. Pure test refactor; no behavior change; hold for a slow round.
25. **AI-path tests for the metric branches (filler, score, pace) routed through `userPrompt(from:)`.** Carried forward from round 44. Hold for the right round.
26. **Cross-surface contract for the consecutive-clean-reps / personal-best / filler-trend momentum lines.** Note from round 45. Hold for the right round.
27. **End-to-end cross-surface contract that routes through `PracticeSessionFinalizer.recordPostRepCoachNote`.** Note from round 45. Higher complexity; hold for a build-host-equipped round.
28. **Mixed-just-finished thin + own-scenario crossing-elsewhere-in-history cross-surface contract.** Note from round 46. Hold for the right round.
29. **Helper-output assertion on the thin-A round-46 leg.** Partially carried over from rounds 48/49. Round 49 covered the round-47 50%-alternating leg and the round-48 boundary-fixture legs. The remaining leg is `MomentumComputer.imToneDrillNoteFields(forJustFinished:in:).progress == nil && .resolved == nil` for the thin-A justFinished (the round-46 thin-A fixture). A defensive test extending the round-49/50 helper-boundary pattern to the thin-A fixture would close the remaining leg of the original #29. Hold for the right round.
30. **Stalled-trajectory window-size = 2 vs window-size = 3 cross-surface contract.** Carried forward from round 49. The round-47/48/49/50 stalled fixtures all use 4 evaluated reps (`window = min(3, 4/2) = 2`). The next granularity is 6+ reps (`window = min(3, 6/2) = 3`), which exercises the wider-window stalled gate. Round 49/50 makes the window-boundary regression more catchable (a regression that shifted the window-size bound from 2 to 3 on a 4-rep fixture would trip the round-49/50 `windowSize == 2` assertions), but the wider-window stalled gate on a 6-rep fixture is still uncovered. Hold for the right round.
31. **Stalled-trajectory at the boundary of `toneDrillProgressThreshold`.** Carried forward from round 49. Hold for the right round.
32. **Stalled-A + parallel-thin-B cross-surface contract.** Carried forward from round 49. Defensive companion; hold for the right round.
33. **Helper-boundary assertion on the parallel-loud fixtures (slipping-B / recovering-B / prior-crossing-C / thin-B).** Carried forward from round 49. The round-47/48 cross-surface tests assert the *surface outputs* don't name the parallel scenario when the just-finished rep is in A; a defensive helper-boundary test would assert the helper output (`MomentumComputer.imToneDrillNoteFields(forJustFinished:in:)`) reads from scenario A's history only — `scenarioTitle == "Networking"`, never `"Difficult Conversation"` or `"Work Update"`, regardless of how loud the parallel scenario's history is. The round-43 `helperOnlyReadsJustFinishedScenarioForCrossingHistory` test covers the crossing case for the SOLVED branch; the symmetric companions for the trajectory branch are still uncovered. Hold for the right round.
34. **Recovering-trajectory boundary at the threshold (`delta == +toneDrillProgressThreshold` exactly).** New note from round 50. The round-44/50 recovering fixture has `delta == 0.5` (well above the 0.15 threshold). A defensive helper-boundary test at the exact recovering boundary (`delta == toneDrillProgressThreshold` and `delta == toneDrillProgressThreshold - ε`) would lock the recovering gate on its tightest discriminator — mirroring future move #31's stalled-boundary companion for the recovering corner. Hold for the right round.
35. **Slipping-trajectory boundary at the threshold (`delta == -toneDrillProgressThreshold` exactly).** New note from round 50. Symmetric companion to move #34 for the slipping corner; the round-45/50 slipping fixture has `delta == -1.0` (deep below threshold). Hold for the right round.
36. **Resolved-side hold-rate boundary (`recentRate == toneDrillResolvedHoldRate` exactly).** New note from round 50. The round-50 recovering fixture sits at `recentRate == 0.5`, below the 0.6 hold rate, so the SOLVED hold-rate arm gates off. A defensive helper-boundary test at the exact boundary (`recentRate == 0.6` and `recentRate == 0.6 - ε`) would lock the SOLVED hold-rate gate on its tightest discriminator. Hold for the right round.
37. **Resolved-side earlier-below-bar boundary (`earlierRate == matchRateThreshold` exactly).** New note from round 50. The round-50 slipping fixture sits at `earlierRate == 1.0`, well above the 0.4 threshold, so the SOLVED earlier-below-bar arm gates off. A defensive helper-boundary test at the exact boundary (`earlierRate == 0.4` and `earlierRate == 0.4 - ε`) would lock the SOLVED earlier-below-bar gate on its tightest discriminator. Hold for the right round.

## Build-host limitation (honest note for the next agent)

This environment has **no Xcode and no Swift toolchain**, so nothing in this round was compiled or run — not the app, not the test suite. The change is:

- Three new `@Test` methods (`helperReturnsRecoveringDirectionForRoundFortyFourRecoveringFixture`, `helperReturnsSlippingDirectionForRoundFortyFiveSlippingFixture`, `helperReturnsResolvedAndRecoveringWithBoundaryRatesForRoundFortyFiveCrossingFixture`) appended to the existing `PostRepCoachNoteUserPromptAnchoringTests` suite in `NoumTests/NoumTests.swift`. Slotted immediately after the existing round-49 `helperReturnsStalledDirectionForFullyOnBrand100PercentStalledFixture` test and immediately before the suite's closing brace, under a new `// MARK: - Helper-level direction assertions on the round-44/45 recovering / slipping / crossing fixtures (round 50)` section. The recovering-A and slipping-B fixtures are built inline using the same shape the round-44/45 surface tests use; the crossing-A fixture uses the suite's `crossingScenarioSessions` helper unchanged.

All checks the next agent should run on a real build host:

1. `swift test --filter PostRepCoachNoteUserPromptAnchoringTests` — the suite now has 30 tests (round-44's 12 + round-45's 3 + round-46's 3 + round-47's 3 + round-48's 3 + round-49's 3 + round-50's 3); all should pass.
2. `swift test --filter PostRepCoachNoteJustFinishedRepAnchoringTests` — round-43's 14 tests should still pass verbatim (the round-50 additions are scoped to the round-44/45/46/47/48/49 suite).
3. `swift test --filter PostRepCoachNoteToneTrajectoryTests` — round-15's existing tests (including the deterministic-path stalled fall-through) should still pass verbatim.
4. `swift test --filter PostRepCoachNoteToneResolvedTests` — the existing SOLVED-branch tests should still pass verbatim.
5. `swift test --filter ToneDrillTrajectoryFreshnessTests` — round-42's 15 tests should still pass verbatim.
6. `swift test --filter ToneDrillSolvedFreshnessTests` — round-41's 11 tests should still pass verbatim.
7. `swift test --filter ToneDrillTrajectoryContextTests` — round-12's 8 tests should still pass.
8. `swift test --filter ToneDrillResolvedContextTests` — round-13's 4 tests should still pass.
9. `swift test --filter IMToneDrillSignalTests` — round-12/14's primitive tests should still pass.
10. `swift test --filter InterventionUnderPushbackContinuationTests` — round-40's 29 tests should still pass.
11. `swift test --filter CaseAnchoredContinuationTests` — round-39's 28 tests should still pass.
12. `swift test --filter CaseAnchoredAmplificationTests` — round-37's 27 tests should still pass.
13. `swift test --filter InterventionUnderRepeatedPushbackTests` — round-38's 32 tests should still pass.
14. `swift test --filter RebuildVerdictContextTests` — round-32's 25 tests should still pass.
15. `swift test --filter FreshRevisedReadContextTests` — round-31's 16 tests should still pass.
16. Confirm the round-21 SOLVED ribbon on the hero score card fires on the round-50 crossing fixture and does NOT fire on the round-50 recovering / slipping fixtures — same primitive (`toneDrillCrossing` → `toneDrillResolved`) routes both surfaces through the helper; round 50 asserts the helper's `resolved != nil` on the crossing fixture and `resolved == nil` on the recovering / slipping fixtures so the two surfaces remain in lockstep with the ribbon.
17. Confirm the round-26 hypothesis-ack chip, round-32 rebuild-verdict line, round-37/38/39/40 case-state matrix, round-31 fresh-revised-read context block, round-41 SOLVED freshness window, round-42 TRAJECTORY freshness window, round-43 deterministic-path anchoring, round-44 AI-path anchoring, round-45 cross-surface SOLVED contract, round-46 cross-surface thin-just-finished contract, round-47 cross-surface stalled-just-finished contract, round-48 cross-surface stalled-just-finished boundary completion contract, round-49 helper-level direction assertions on the round-47/48 stalled fixtures, and round-50 helper-level direction + boundary-rate assertions on the round-44/45 recovering / slipping / crossing fixtures are all preserved verbatim.
